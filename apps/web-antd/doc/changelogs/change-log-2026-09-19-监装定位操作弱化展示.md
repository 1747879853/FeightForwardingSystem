# 监装定位操作弱化展示

## 背景意图

定位重试与权限设置是辅助操作，原生按钮整行铺开、边框及字号过重，挤占照片上传区域。

## 核心逻辑变更

- 详情页和照片面板统一在定位提示下方并排展示小号文字操作，窄屏允许换行。
- 使用 24rpx 常规字重，去掉原生按钮背景、边框及伪元素描边，保留按钮语义与点击事件。
- 点击区域保持至少 64rpx 高；权限恢复与定位逻辑不变。

## 避坑指南

微信原生 button 的描边包含 ::after，仅设置 border: 0 不足，需要一并清除。

## 验证

两个组件样式检查与小程序构建通过；实际设备视觉待验证。
