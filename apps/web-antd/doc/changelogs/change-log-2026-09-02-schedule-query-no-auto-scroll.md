# 船期查询点击查询后不再自动滚动

## 背景意图

点「查询」或最近航线胶囊后，页面会把方案列表滚进视口。查询条已经吸顶，结果就在下方，自动滚动反而打断当前视口。

## 核心逻辑变更

- `handleQuery` 成功后不再 `scrollIntoView`。
- 去掉只为滚动服务的 `schemeListRef`。

## 避坑指南

- 查询条吸顶、列表在 `.schedule-main` 内滚动的布局不要改回去；没有自动滚时，用户仍能立刻看到方案卡。
