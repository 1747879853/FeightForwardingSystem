# 2026-08-23 监装师傅取消前端人数上限

## 背景意图

后端 `userIds` 不卡人数。产品要求前端也不再截成最多 2 人，选几个传几个。

## 核心逻辑变更

- 去掉 `UserSelect` 选第 3 人时的截断与提示。
- 「推荐」回填不再只带前 2 个师傅。
- `userIds` 数组顺序仍是监装顺序，`sortId` 仍由后端按数组下标生成。

## 避坑指南

- 不要再加前端人数上限，除非产品重新要求。
