# 监装列表选中 Tab 与卡片断层补上

## 背景意图

加过渡色条时白滑块只画到 88、列表又空了 20，选中态和第一张卡片中间露出底色，看起来断层。

## 核心逻辑变更

- 白滑块重新画满 114rpx 轨道底边，只裁顶部圆角，选中态穿过错开的浅蓝带。
- 列表 `margin-top: -26rpx` 叠在轨道下沿（稿：卡片从 Tab 顶 88、轨道 114），白滑块和内容连上；文案仍画在上半 88rpx。

## 避坑指南

- 过渡条只应出现在未选中段下方，不能整宽切断选中白块。
- 四角都裁圆角会在轨道底边再挖一条缝，连内容时只裁顶角。
