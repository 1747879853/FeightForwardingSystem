# 2026-09-09 海运出口分单去掉打印按钮

## 背景意图

分单 Tab 顶栏「打印」实际走海出主单 `PrintJsonType=0`，打的是整票不是当前分单，入口没有用。

## 核心逻辑变更

- 去掉分单顶栏打印按钮及相关 `openPrint` 调用。
- 顶栏操作保留删除、复制、保存。

## 避坑指南

- 基础信息 Tab 的打印不受影响。分单要单独打，需要分单数据源和独立入口，不要再挂回主单打印。
