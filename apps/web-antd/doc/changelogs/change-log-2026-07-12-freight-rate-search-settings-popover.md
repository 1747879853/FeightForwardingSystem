# 运价列表搜索项设置弹层恢复显示

## 背景意图

运价列表工具栏的「搜索项设置」按钮点击后没有可见反馈，导致用户无法调整搜索字段的显示与顺序。

## 核心逻辑变更

- 将运价列表 VXE 工具栏的溢出行为由裁剪改为可见。
- 保留航线 Tab 自身容器的横向溢出控制，不影响航线较多时的滚动浏览。
- 搜索项设置弹层现在可以正常显示在工具栏下方。

## 避坑指南

1. 工具栏内包含绝对定位弹层时，不要在弹层祖先节点设置 `overflow: hidden`，否则即使点击状态已切换，弹层仍会被裁掉。
2. 横向滚动应限制在航线 Tab 容器内部，不应通过裁剪整个工具栏实现。
