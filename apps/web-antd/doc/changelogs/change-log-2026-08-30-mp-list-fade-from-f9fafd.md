# 监装列表过渡渐变从 #F9FAFD 起

## 背景意图

选中滑块已是 `#F9FAFD`，下列表渐变仍从 `#FDFEFF` 起会对不上。起点改成同一色，衔接才自然。

## 核心逻辑变更

- `.list__fade` 改为 `linear-gradient(180deg, #F9FAFD 0%, #F0F2F8 52.55%)`。

## 避坑指南

- 以后改选中滑块色时，渐变起点要一起改。
