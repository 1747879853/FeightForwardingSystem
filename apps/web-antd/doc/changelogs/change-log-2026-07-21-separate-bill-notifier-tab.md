# 分单通知人/第二通知人改为标签切换

## 背景意图

通知人与第二通知人并排占高，且与基础信息收发通交互不一致。改为同一槽位标签切换，默认通知人。

## 核心逻辑变更

1. 标签：`通知人` / `第二通知人`（样式对齐基础信息 `transit-port-tabs`）。
2. 通知人：分单可编辑；第二通知人：主单只读，切换时旁注「来自主单，只读」。
3. 打开新增/编辑弹窗时重置为通知人页签。

## 避坑指南

- 第二通知人仍不进入分单保存体。
- 目的港代理仍在右侧独立区块，未并入该切换（与基础信息三页签不同）。
