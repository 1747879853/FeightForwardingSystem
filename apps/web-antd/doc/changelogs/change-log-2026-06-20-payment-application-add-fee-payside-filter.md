# 付费申请添加费用抽屉增加收付类型筛选

## 背景意图

`GetOrderFeeGroupAsync` 支持 `paySide` 参数（0 收 / 1 付），付费申请选费抽屉需暴露该筛选条件。

## 核心逻辑变更

1. 搜索区新增「收付类型」下拉，选项为收/付，默认「付」。
2. 清空时不传 `PaySide`，返回收、付全部费用。
3. 条件变更纳入搜索快照，切换收付类型会清空当前勾选。

## 避坑指南

- `PaySideOptions` 需在 `useAddFeeSearchSchema` 之前定义，避免运行时引用错误。
