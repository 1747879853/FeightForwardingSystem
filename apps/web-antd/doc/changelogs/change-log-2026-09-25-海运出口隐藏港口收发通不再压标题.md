# 海运出口隐藏港口与收发通不再压标题

## 背景意图

基础信息里折叠的中转港、收发通仍占着位置，中转港 2 会压住中转港 1 的标题。

## 核心逻辑变更

隐藏态选择器改为 `display: none !important`。表单项自身带 flex，普通 `display: none` 盖不住。

## 避坑指南

港口流转与收发通两处隐藏类都要加 `!important`，只改一处时另一处仍会叠标题。
