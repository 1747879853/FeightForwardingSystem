# 业务联系单附件分组支持拖拽上传

## 背景意图

- TAPD #0718：附件要能把文件拖到分组卡片上直接上传，不必只点按钮。

## 核心逻辑变更

- 分组卡片支持 `dragover`/`drop`，可一次拖入多个文件。
- 空态文案改为「点击或拖拽上传」；只读态仍为「暂无文件」。
- 多文件串行上传时按最新 model 追加，避免后一个覆盖前一个。
- 顶栏按钮新建为「上传附件」，编辑（含待审核/通过）为「查看附件」；弹窗内录入/驳回仍可上传。

## 避坑指南

- 只处理 `dataTransfer.types` 含 `Files` 的拖入，避免页面内拖文字也高亮。
