# 海运出口业务来源改为可下拉选择

## 背景意图

- TAPD #0805：业务来源要能下拉选，基础资料已有维护页。

## 核心逻辑变更

- 头部只读文案改回 `CodeSourceSelect`，与装运方式同一行。
- 选委托单位仍按客户维护值自动带出，允许再改或清空。
- 表单隐藏字段 `codeSourceId` 仍随头部写入，保存口径不变。

## 避坑指南

- 换委托单位会覆盖当前已选来源；若要保留手选，需换完客户后再选一次。
