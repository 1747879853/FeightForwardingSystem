# 海出监装去掉单独查看视频按钮

日期：2026-09-13

## 背景意图

海出编辑页「监装」Tab 工具栏单独放了「查看监装视频」，和「分享」预览里的客户页入口重复。

## 核心逻辑变更

- 管理端工具栏只保留「分享」。看直播走分享预览里的「查看监装视频」，不再单独渲染按钮。
- 摄像头名称仍只读展示；删除已无引用的 `viewLiveVideo` 文案。

## 避坑指南

- 不要在管理端再加一套点播入口。预览弹窗已经嵌入公开页，点那里的按钮才会占观看位。

## 验证

代码核对工具栏只剩分享；公开页直播入口未改。
