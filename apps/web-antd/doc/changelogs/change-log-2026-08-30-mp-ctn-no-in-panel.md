# 箱号封号改到监装处理面板集中填写

## 背景意图

箱号、封号原先写在集装箱表格单元格里，和状态、照片分散。希望进「监装处理」一次改完。

## 核心逻辑变更

- 面板在监装状态下增加箱号、封号输入（已认领可改，其它状态只读）。
- 详情表格这两列改为只展示。改动仍走详情页底「保存」，对象还是同一份 `ctns`。

## 避坑指南

- 输入框不要加 `width: 100%` + padding，用 flex 行布局即可。
