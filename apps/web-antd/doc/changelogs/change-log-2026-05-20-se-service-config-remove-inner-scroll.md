# 2026-05-20 海运出口港口服务项明细移除内层滚动条

## 背景意图

- 服务项配置弹窗内“明细列表”与外层弹窗同时存在纵向滚动，形成双滚动条体验。
- 双滚动会增加鼠标滚轮与触控板的操作成本，影响连续编辑的流畅性。

## 核心逻辑变更

- 移除服务项明细容器的 `max-h-[58vh]` 与 `overflow-auto`。
- 明细区域改为跟随外层弹窗统一滚动，只保留单一纵向滚动通道。

## 避坑指南

- 若后续明细项进一步增多，优先维持“单滚动”原则，避免再次引入内外双滚动冲突。
- 如需控制性能或首屏长度，建议通过分段折叠/分页而不是叠加额外滚动容器。
