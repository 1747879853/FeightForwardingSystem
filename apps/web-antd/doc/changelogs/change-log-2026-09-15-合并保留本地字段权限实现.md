# 合并保留本地字段权限实现

## 背景意图

本地通用字段权限与远端海出专用实现同时修改海出表单、列配置和费用表。按用户明确要求，字段权限完全采用合并前本地 HEAD 的实现。

## 核心逻辑变更

- 远端 `f7a5d8d96` 涉及的 18 个字段权限代码、测试和文档文件均保持本地 HEAD 内容，不叠加远端专用实现。
- 保留本地通用权限模块及挂载前访问表单 API 的修复，覆盖海出、海进、空出、费用和运价。
- 正常合入远端其余功能：客户开票 AI 识别、客户干系人样式、用户下拉展示、操作提成回显及付费结算类型展示等。

## 避坑指南

后续字段权限扩展继续使用 `composables/field-permission.ts`、`field-permission-profiles.ts` 和 `use-field-permission.ts`，不要重新引入已舍弃的海出专用适配层。

## 验证

本地字段权限 15 项测试通过；其余合入的 9 个 Vue 文件通过脚本与模板编译；无冲突标记，暂存差异检查通过。全项目类型检查仍有历史诊断，未完成浏览器端到端验收。
