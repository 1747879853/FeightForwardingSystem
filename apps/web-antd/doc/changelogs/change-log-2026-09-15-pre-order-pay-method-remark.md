---
title: 业务联系单付款方式前移与备注后置
date: 2026-09-15
module: pre-order
---

# 背景意图

TAPD `#1161580498001000965`：船名/航次按海出合并输入放在船公司后；付款方式挪到贸易条款前；订舱代理后加车队；备注放到车队后面，不再做高亮。

# 核心逻辑变更

1. **船名/航次、车队：** 沿用 2026-09-14 已落地的 `VesselVoyageInput` 与 `teamId`，本轮不改取值。
2. **付款方式前移：** 次行改为起运地 → 目的地 → 付款方式 → 贸易条款/运输条款。
3. **备注后置：** 订舱代理、车队补上次行空位；备注在车队后换行通栏，默认 3 行，普通样式，不做黄底/加粗。

# 避坑指南

- 次行 6 项会把订舱代理、车队挤到贸易条款右侧；备注必须 `col-span-6` 换行，不要再塞回次行。
- 只改主表 `remark`，不要动港口备注、费用行备注、箱型行备注。
- `VesselVoyageInput` 的 `componentProps` 必须是函数，否则航次回显会丢。
