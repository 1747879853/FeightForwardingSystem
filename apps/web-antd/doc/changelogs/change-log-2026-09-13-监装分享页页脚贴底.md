# 监装分享页页脚贴底

日期：2026-09-13

## 背景意图

短工单时「津海通 · 监装信息共享」跟在卡片下面，悬在半空。页脚应贴在窗口底部。

## 核心逻辑变更

- 页面改成顶栏 / 可滚动主体 / 页脚三截。内容不够高时页脚贴视口底；照片多了只滚中间，页脚仍在底。
- 白卡片高度仍跟内容走，不拉满。

## 避坑指南

- 不要把页脚做成 `position: fixed` 盖住照片。
- 不要给卡片 `flex: 1` 撑满视口，短工单会再出现大块白底。

## 验证

1600×900 拦截 jz0015：页脚底边贴齐视口，卡片约 274px 高，字段仍是 5 列。
