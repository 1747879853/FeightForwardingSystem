---
title: TAPD 1000122 监装联调测试数据
dataset_id: tapd-1000122-loading-supervision
tapd: 1161580498001000122
environment: local-dev; jht-180
base_url: http://localhost:5010
jht_url: http://43.138.14.122:180
created_at: 2026-08-22
last_updated: 2026-08-27
maintainer: browser-use 造数
keyword: 测试
---

# TAPD #1000122 监装 — 联调测试数据

跨会话复用这份清单，**不要另起一套名字**。  
查找：各列表关键字搜「测试」。清库后把下方状态改回「待建」再重建。

计划总览：[00-总览.md](../plans/tapd-1000122-loading-supervision/00-总览.md)

| 项       | 值                                                              |
| :------- | :-------------------------------------------------------------- |
| 环境     | 本地 `http://localhost:5010`；津海通 `http://43.138.14.122:180` |
| 造数方式 | browser-use 操作管理端 UI                                       |
| 名称前缀 | **测试**（排序主表用 `910` / `920`，排到正式资料后面）          |
| 主键     | 以**名称**为准；雪花/Guid 换库后失效，需要时再从详情抄          |

## 串联关系（后续模块必须用这些）

```text
包装「测试纸箱」── 测试A型纸箱 ──────────────┐
                                          ├─ 海出「测试票1」── 监装工单
船公司「测试中远」── 测试外高桥一期堆场 ─────┘        │
                                                   ├ 师傅：测试张监装、测试李监装
用户 测试张监装 / 测试李监装 ───────────────────────┘
监装要求「测试标准整箱监装要求」── 勾前 3 条 ────────┘
编号规则 LoadingOrder.LoadingOrderNum ───────────── 工单号
```

工单主路径建议值：明细包装 `测试A型纸箱`、件数 `400`、预计到货 `2026-08-25 09:00`、堆场 `测试外高桥一期堆场`、师傅先张后李、要求勾标准整箱前 3 条。

---

## 1. 包装类型（已落库 2026-08-22）

页面：`/basic-data/code-package`  
按钮：新增包装类型

### 1.1 测试纸箱（海出主单用）

| 主表      | 值                  |
| :-------- | :------------------ |
| 状态      | **已落库**          |
| 名称      | `测试纸箱`          |
| 描述      | `测试-通用纸质包装` |
| AFR / EDI | `CT` / `CT`         |
| 启用      | 是                  |
| 排序      | `910`               |
| 备注      | `测试数据`          |

| 明细名称      | 备注        |
| :------------ | :---------- |
| `测试A型纸箱` | `测试-小箱` |
| `测试B型纸箱` | `测试-中箱` |
| `测试C型纸箱` | `测试-加固` |

### 1.2 测试木箱（负向：不属于主单包装）

| 主表 | 值         |
| :--- | :--------- |
| 状态 | **已落库** |
| 名称 | `测试木箱` |
| 排序 | `920`      |
| 备注 | `测试数据` |
| 启用 | 是         |

| 明细名称       | 备注   |
| :------------- | :----- |
| `测试出口木箱` | （空） |
| `测试熏蒸木箱` | （空） |

海出主单选测试纸箱后，工单明细下拉只能出现 A/B/C 型纸箱。

---

## 2. 监装要求（本地已落库 2026-08-22；津海通已落库 2026-08-27）

页面：`/basic-data/loading-requirement`  
按钮：新增监装要求  
列表默认按主表 `sortId` 升序。

津海通 [监装要求](http://43.138.14.122:180/#/basic-data/loading-requirement) 于 2026-08-27 按下列建议值新建；列表关键字「测试」共 2 条，创建人「超级管理员」。

### 2.1 测试标准整箱监装要求（工单主路径勾选）

| 主表   | 值                                               |
| :----- | :----------------------------------------------- |
| 状态   | **已落库**（本地 2026-08-22；津海通 2026-08-27） |
| 要求名 | `测试标准整箱监装要求`                           |
| 排序   | `910`                                            |
| 备注   | `测试-普货整箱`                                  |
| 明细数 | 4                                                |

| 明细名称 | 备注 | 工单主路径 |
| :-- | :-- | :-- |
| `测试装箱前拍空箱内壁照` | `四壁各一张` | 勾 |
| `测试货物码放需加护角` | `四角均需加` | 勾 |
| `测试装完贴封条并拍照` | `封条号需清晰` | 勾 |
| `测试核对箱号与封号` | `与设备交接单一致` | 不勾（用来测 isChecked 回填） |

### 2.2 测试危险品监装要求（跨要求同名允许）

| 主表   | 值                                               |
| :----- | :----------------------------------------------- |
| 状态   | **已落库**（本地 2026-08-22；津海通 2026-08-27） |
| 要求名 | `测试危险品监装要求`                             |
| 排序   | `920`                                            |
| 备注   | `测试-危品加拍`                                  |
| 明细数 | 3                                                |

| 明细名称                 | 备注                  |
| :----------------------- | :-------------------- |
| `测试核对危标与隔离`     | `UN 号可见`           |
| `测试装箱前拍空箱内壁照` | 与 2.1 同名，**允许** |
| `测试装完贴封条并拍照`   | 同名允许              |

工单勾选树应先出标准整箱（910）再出危险品（920）。

---

## 3. 船公司 + 堆场

页面：`/basic-data/carrier`  
同一船公司下堆场名唯一（去空格、忽略大小写）。

- **中远**：仍按 3.1 **新建** `测试中远`（不要往正式中远上堆测试堆场）。
- **马士基**：已按用户要求挂在正式船公司「马士基」（MSK）上，**不另建** `测试马士基航运`。负向用例（堆场不属于本票船公司）用这些测试堆场即可。

### 3.1 测试中远（海出主单用）

| 主表       | 值                    |
| :--------- | :-------------------- |
| 状态       | 待建                  |
| 中文名称   | `测试中远海运`        |
| 中文简称   | `测试中远`            |
| 英文名称   | `TEST COSCO Shipping` |
| 英文简称   | `TESTCOSCO`           |
| 别名 / EDI | `TCOS` / `TCOSU`      |
| 备注       | `测试数据`            |

| 堆场名称             | 地址                                    | 备注        |
| :------------------- | :-------------------------------------- | :---------- |
| `测试外高桥一期堆场` | `上海市浦东新区外高桥保税区港通路 1 号` | `测试-常用` |
| `测试洋山深水港堆场` | `上海市浦东新区洋山深水港物流园区 8 号` | `测试-远洋` |
| `测试宝山罗泾堆场`   | `上海市宝山区罗泾镇港口路 9 号`         | `测试-备选` |

### 3.2 马士基（负向：堆场不属于本票船公司）

挂在**已有**船公司上，不要新建「测试马士基航运」。列表有两条马士基相关记录，用简称「马士基」、英文简称 `MSK` 的那条（不要点 MCC / 穆勒亚洲）。

| 主表       | 值                                   |
| :--------- | :----------------------------------- |
| 状态       | **已落库 2026-08-22**                |
| 中文名称   | `马士基（中国）航运有限公司`         |
| 中文简称   | `马士基`                             |
| 英文名称   | `MAERSK（SEALAND）SHIPPING CO.,LTD.` |
| 英文简称   | `MSK`                                |
| 别名 / EDI | `MSK` / `MSK`                        |

| 堆场名称 | 地址 | 备注 |
| :-- | :-- | :-- |
| `测试外高桥马士基堆场` | `上海市浦东新区外高桥港区 5 号门` | `测试-马士基专用` |
| `测试宁波北仑堆场` | `浙江省宁波市北仑区港东路 88 号` | `测试-宁波线` |
| `测试洋山马士基堆场` | `上海市浦东新区洋山深水港物流园区` | `测试-远洋` |

原先这张船公司没有堆场；本次只**追加**上述 3 行，主表未改。海出若选测试中远，工单堆场下拉不应出现这 3 条。

---

## 4. 用户（待建）

页面：`/system/user`  
筛监装：`userAttribute=512`（按位与，不要 `=== 512`）。  
默认密码以环境为准（常见 `123qwe` / `123123`），建完后把实际密码写进本表。

| 状态 | 登录名          | 昵称         | 用户属性        | 用途             |
| :--- | :-------------- | :----------- | :-------------- | :--------------- |
| 待建 | `test_jz_zhang` | `测试张监装` | 监装 `512`      | 工单主师傅       |
| 待建 | `test_jz_li`    | `测试李监装` | 监装 `512`      | 第二师傅         |
| 待建 | `test_jz_wang`  | `测试王监装` | 监装+操作 `513` | 组合属性也能筛出 |
| 待建 | `test_op_zhao`  | `测试赵操作` | 只操作 `1`      | 负向：不能当师傅 |

---

## 5. 编号规则（待建）

页面：`/basic-data/generate-num`  
若租户**已有** `tableName = LoadingOrder.LoadingOrderNum`，不要再建，把状态改成「沿用已有」并记下规则名。

| 字段            | 建议值                         |
| :-------------- | :----------------------------- |
| 状态            | 待建                           |
| 规则名称        | `测试监装工单号`               |
| 表名            | `LoadingOrder.LoadingOrderNum` |
| 组织 / 适用用户 | 空（全公司）                   |
| 规则 1          | 固定字符串 `TESTJZ`，长度 6    |
| 规则 2          | 自动编号，长度 4，变化后重置   |

生成类似 `TESTJZ0001`。未配会报 `未配置生成规则`。

---

## 6. 海运出口票（待建）

页面：`/sea-exports/create`  
**必须先保存**再进监装 Tab（未保存的包装/船公司不会进下拉）。备注/关键字写 `测试监装`。

| 票               | 状态 | 包装     | 船公司   | 箱子  | 用途         |
| :--------------- | :--- | :------- | :------- | :---- | :----------- |
| 测试票1 完整     | 待建 | 测试纸箱 | 测试中远 | ≥1 箱 | 工单主路径   |
| 测试票2 无包装   | 待建 | 空       | 测试中远 | 有箱  | 明细包装禁用 |
| 测试票3 无船公司 | 待建 | 测试纸箱 | 空       | 有箱  | 堆场禁用     |
| 测试票4 无箱子   | 待建 | 测试纸箱 | 测试中远 | 0 箱  | 提交被拦     |

落库后把委托编号、海出 id（string）补到本表。

---

## 7. 监装工单（待建，依赖 1–6）

页面：海出编辑 → Tab「监装工单」（派车之后、分单之前）

| 字段     | 测试票1 建议值              |
| :------- | :-------------------------- |
| 状态     | 待建                        |
| 明细包装 | `测试A型纸箱`               |
| 件数     | `400`                       |
| 预计到货 | `2026-08-25 09:00`          |
| 监装堆场 | `测试外高桥一期堆场`        |
| 监装师傅 | `测试张监装` → `测试李监装` |
| 监装要求 | 勾 2.1 前 3 条，危险品不勾  |

两条提交路径：有师傅 → 已认领；师傅留空 → 待认领（可撤回）。

---

## 变更记录

| 日期       | 动作                                                           |
| :--------- | :------------------------------------------------------------- |
| 2026-08-27 | 津海通 `:180` 监装要求页新建 2.1 / 2.2（原先该环境列表为空）   |
| 2026-08-22 | 正式「马士基」(MSK) 追加 3 条测试堆场；不另建测试马士基航运    |
| 2026-08-22 | 浏览器创建包装 1.1 / 1.2、监装要求 2.1 / 2.2；测试中远等仍待建 |
