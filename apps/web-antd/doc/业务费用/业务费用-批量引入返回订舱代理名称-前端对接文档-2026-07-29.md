# 业务费用批量引入 — 返回订舱代理名称 前端对接

**日期：** 2026-07-29  
**接口：** `GET` `/api/services/app/OrderFeeAdmin/GetSeaExportFeesAsync`（以实际动态 API 路由为准：`OrderFeeAdmin/GetSeaExportFees`）

## 出参变更

子对象 `transportOrder`（`SeaExportFeeTransportOrderDto`）新增：

| 字段名 | JSON Key | 类型 | 说明 |
| :-- | :-- | :-- | :-- |
| BookingAgentName | `bookingAgentName` | `string` / null | 订舱代理名称（来自 `SeaExport.BookingAgentId` 对应客户 `Name`；未维护时为 null） |

其余字段不变。
