---
title: 风鸟(RiskBird)企业查询 - 账号池与登录态持久化
module: 外部Api对接
author: auto-doc-sync
last_updated: 2026-06-29
---

# 1. 背景意图 (Background)

此前 `RiskbirdAdminAppService` 把单一账号密码硬编码在代码中，登录态（token + Cookie）仅保存在进程内的 `static` 字段。该方案存在三个问题：

1. 只能配置一个账号，无法多账号轮换使用。
2. 登录态随进程重启丢失，且多实例部署时无法共享。
3. 并发首登时多个请求会同时判定“未登录”，对同一账号重复登录。

本次改造：将账号放入数据库（支持多账号），查询时由调用方选择某个账号；当该账号未登录或 token 失效时，对该账号数据行加悲观锁串行重新登录，并把 token 与 Cookie 回写数据库，避免并发重复登录。

# 2. 核心逻辑变更 (Core Changes)

## 2.1 新增实体与表

`src/CsprojBuilder.Core/Entites/RiskbirdAccount.cs`（表 `App_RiskbirdAccounts`，`FullAuditedEntity<long>`）

- `Name` 别名、`UserName` 账号、`Password` 密码。
- `Token` 当前 token、`CookieJson` 完整 Cookie 字典(JSON)、`TokenExpireTime` 过期时间、`LastLoginTime`、`IsEnabled`、`Remark`。
- DbContext 注册 `DbSet<RiskbirdAccount> RiskbirdAccounts`。
- 迁移文件：`20260621090414_riskbirdaccount`。

## 2.2 DTO 变更

`src/CsprojBuilder.Application/App/Riskbird/Dto/RiskbirdDto.cs`

- `RiskbirdCompanySearchDto`、`RiskbirdCompanyDetailInputDto` 新增 `long AccountId`（选择使用的账号）。
- 新增 `RiskbirdAccountDto`（列表项，含 `IsLogged` 登录态是否有效，不含密码/token）。
- 新增 `RiskbirdAccountSaveDto`（新增/编辑账号入参，编辑时密码留空表示不改）。

## 2.3 服务逻辑

`src/CsprojBuilder.Application/App/Riskbird/RiskbirdAdminAppService.cs`

- 删除所有 `static` 登录态字段与硬编码账号；改为注入 `IRepository<RiskbirdAccount, long>`。
- `EnsureAccountReadyAsync(accountId)`：
  - 快路径：`AsNoTracking` 读取账号，token 有效（存在且未过期）直接复用，**不加锁**。
  - 慢路径：开启 `ReadCommitted` 事务，`FromSqlRaw("... WITH (UPDLOCK, ROWLOCK) WHERE Id = {0}")` 对该行加悲观锁；拿锁后**二次检查**（其他线程可能已重登则直接复用），仍失效才真正登录并回写 `Token/CookieJson/TokenExpireTime/LastLoginTime`。
- `SendWithReloginAsync`：统一发请求；当返回 HTTP 401 或业务体判定登录态失效时，先 `InvalidateTokenIfMatchAsync` 作废 token，再 `EnsureAccountReadyAsync` 重登并重试一次。
- `InvalidateTokenIfMatchAsync`：同样加行锁，且**仅当库中 token 与本次失败 token 相同时**才清空，避免误清其他线程刚刷新的有效 token。
- Cookie 与登录改为按账号传递 `Dictionary<string,string>`（`CreateConfiguredClient`/`LoginAsync`/`CaptureCookies`/`BuildCookieHeader` 均带参），`BuildDeviceCookies` 复用历史 `app-uuid` 保持设备标识稳定。
- 新增账号管理接口：`GetAccountListAsync`、`SaveAccountAsync`、`DeleteAccountAsync`。

# 3. 避坑指南 (Pitfalls)

- **并发重复登录**：核心在于“锁内二次检查 + token 比对作废”。只要复用现有 `UPDLOCK, ROWLOCK` 行锁模式（与 `App_GenerateNumRuleNums`、`App_OrderFees` 一致），同一账号并发只会重登一次。
- **不要无条件清 token**：401 重试时若直接清空 token，会把其他线程刚登好的有效 token 误删，因此用 `Token == failedToken` 守卫。
- **token 与 Cookie 必须同源保存**：风鸟鉴权同时依赖 Cookie 中的 `token`，所以整份 Cookie 以 `CookieJson` 持久化，重登时基于 `BuildDeviceCookies` 重新发起以丢弃过期鉴权 Cookie。
- **详情页失效判定**：详情接口返回 HTML 而非 JSON，无法用 `code=401` 判断，改用“解析不出 `jbxxInfo` 即视为可能失效”触发一次重登重试。
- **迁移**：`App_RiskbirdAccounts` 表需执行迁移后才存在；账号需先通过 `SaveAccountAsync` 录入后才能在查询中选择。

# 4. 关联客户：按客户查询与工商信息回写

## 4.1 SearchCompanyAsync

- 入参 `RiskbirdCompanySearchDto` 新增可空 `Guid? ClientId`。
- 传入 `ClientId` 时：用 `IRepository<Client,Guid>` 查出客户，取其 `FullName` 作为查询关键字（忽略 `Keyword`）；客户不存在或 `FullName` 为空时报错。未传则继续用 `Keyword`。

## 4.2 GetCompanyDetailAsync

- 入参 `RiskbirdCompanyDetailInputDto` 新增可空 `Guid? ClientId`。
- 详情查询成功且传入 `ClientId` 时，调用 `UpdateClientFromDetailAsync` 把工商信息回写到 `App_Clients`，字段映射如下（**仅当风鸟返回值非空才覆盖**，避免清空已有数据）：

| 序号 | 业务字段 | 风鸟来源(jbxxInfo) | Client 字段 |
| :-- | :-- | :-- | :-- |
| 1 | 纳税人识别号 | `uniscid` | `TaxNo` |
| 2 | 法人 | `personName` | `LegalPerson` |
| 3 | 注册资本 | `regConcat`/`regCap` | `RegisteredCapital` |
| 4 | 成立时间 | `esDate` | `EstablishmentDate`(DateTime) |
| 5 | 营业期限 | `opFrom`/`opTo`(数字时间戳) | `BusinessTerm`(格式化为 "yyyy-MM-dd 至 yyyy-MM-dd/长期") |
| 6 | 地址 | `dom` | `Address` |
| 7 | 英文名称 | `entNameEng` 等多键回退 | `EnName` |
| 8 | 电话 | `tel` | `Phone` |
| 9 | 官网网址 | `webSite`/`website` 等多键回退 | `Url` |
| 10 | 邮箱 | `email` | `Email` |

- 详情 DTO 新增 `EnName`、`Website` 两个字段；新增日期工具 `ParseRiskbirdDate`（兼容毫秒/秒时间戳与日期字符串）、`FormatBusinessTerm`。

> [!IMPORTANT] 风鸟 jbxxInfo 中“英文名称”“官网”的真实字段名暂无样本，代码用多键回退（如 `entNameEng/enterpriseNameEng/englishName/enName`、`webSite/website/webAddr/url`）；抓到真实返回后应按实际键收敛，避免误取。

# 5. 多租户隔离与权限

## 5.1 账号按租户隔离

- `RiskbirdAccount` 实现 `IMustHaveTenant`，新增 `int TenantId`，每个租户维护各自账号池；查询/列表/回写均由 ABP 租户过滤自动隔离。
- ⚠️ 迁移由人工生成（本次未生成迁移文件）。

## 5.2 第三方接口权限

- `FreightPermissions` 新增 `Admin.ExternalApi` 及 `.Add/.Get/.Edit/.Delete/.Use`，在 `FreightAuthorizationProvider` 注册（`MultiTenancySides.Tenant`），`CsprojBuilder-zh-Hans.xml` 汉化为「第三方接口/查看/编辑/添加/删除/使用」。
- `RiskbirdAdminAppService` 接口挂权限：
  - 使用第三方接口（`SearchCompanyAsync`/`GetCompanyDetailAsync`）→ `Admin.ExternalApi.Use`（独立的“使用”权限，与查看分离）
  - `GetAccountListAsync` → `Admin.ExternalApi.Get`
  - `SaveAccountAsync` → `Admin.ExternalApi.Add` 或 `Edit`（`RequireAllPermissions=false`）
  - `DeleteAccountAsync` → `Admin.ExternalApi.Delete`

# 6. 查询接口取消 AccountId 入参，改为随机使用账号（2026-06-29）

- 背景：此前 `SearchCompanyAsync`/`GetCompanyDetailAsync` 要求前端传入 `AccountId` 指定账号，交互繁琐且固定账号易触发风控。本次改为服务端在每次查询时从“启用”的账号池中**随机择一**账号使用。
- DTO 变更（`RiskbirdDto.cs`）：`RiskbirdCompanySearchDto`、`RiskbirdCompanyDetailInputDto` 移除 `long AccountId` 字段。
- 服务逻辑（`RiskbirdAdminAppService.cs`）：
  - 两接口移除 `input.AccountId <= 0` 的“请选择查询账号”校验。
  - 新增私有方法 `PickRandomEnabledAccountIdAsync()`：查出 `IsEnabled = true` 的全部账号 Id，随机取其一返回；账号池为空时报错「没有可用的风鸟账号，请先在账号池中添加并启用账号」。
  - 两接口发起请求前调用该方法取随机账号 Id，再传入 `SendWithReloginAsync`。
- 避坑：随机选号仅在启用账号范围内进行，需保证至少一个启用账号；选中账号登录态失效仍由 `EnsureAccountReadyAsync` 加行锁串行重登，并发安全不受影响；前端调用方需同步移除请求体中的 `AccountId`。

# 7. 变更与解析日志 (Changelog & Insights)

| 日期 | 变更类型 | 📝 业务功能变动 | 🤖 代码解析与架构洞察 |
| :-- | :-- | :-- | :-- |
| 2026-06-21 | `Feature` | 风鸟账号改为数据库多账号管理，查询时选择账号；账号未登录/失效时自动重登并持久化 token+Cookie | 复用项目既有 `UPDLOCK,ROWLOCK` + UoW 事务的悲观锁范式，配合“锁内二次检查 + token 比对作废”实现同一账号并发只重登一次，避免重复登录 |
| 2026-06-21 | `Feature` | 搜索支持按客户Id用其全称查询；详情支持按客户Id回写工商信息(10个字段)到客户表 | 营业期限/成立时间为数字时间戳，统一用 `ParseRiskbirdDate` 兼容毫秒/秒/日期串；回写仅覆盖非空值；`Client` 同时是命名空间与实体名，需用 `Entites.Client` 限定 |
| 2026-06-21 | `Feature` | 账号改为按租户隔离；新增「第三方接口」增删改查权限并汉化，给风鸟接口挂权限（使用类接口挂 .Get） | 沿用 `FreightPermissions` + `FreightAuthorizationProvider` + zh-Hans 三处同步的权限范式；保存为新增/编辑合并接口故用 `RequireAllPermissions=false`；迁移由人工生成 |
| 2026-06-29 | `Feature` | 企业搜索/详情接口取消 `AccountId` 入参，改为服务端随机使用启用账号查询 | 新增 `PickRandomEnabledAccountIdAsync` 仅在启用账号范围内随机择号；复用既有重登与行锁逻辑，对并发安全无影响 |
