---
title: 海运出口港口服务项配置
module: 基础资料
author: auto-doc-sync
last_updated: 2026-05-21
---

# 1. 业务背景说明 (Background)

**白话解释：**  
按起运港维护海运出口服务项模板，定义每个服务项的责任角色、是否允许自动/手动完成以及字段展示、锁定、必填规则，供业务单据执行环节复用。

# 2. 功能与操作说明 (Features & Operations)

- **列表检索：** 在 `/basic-data/se-service-config` 按起运港和服务项类型筛选配置明细。
- **配置维护：** 在弹窗中一次性维护主配置与多条服务项明细，支持新增、编辑、删除。
- **开关紧凑布局：** 服务项内“自动完成 / 允许手动完成 / 完成提醒”使用开关并同排展示，减少纵向滚动。
- **明细标题增强：** 每条服务项标题显示“序号 + 已选服务项类型名称”，便于快速定位当前编辑块。
- **弹窗与标题可读性优化：** 缩减弹窗宽度并强化明细标题视觉层级，提升聚焦效率。
- **标题样式收敛：** 标题改为左侧品牌蓝竖线强调，并移除浅底色与圆角，减少视觉噪声。
- **滚动体验优化：** 移除明细区内层滚动，仅保留外层弹窗滚动，避免双滚动条冲突。
- **明细顺序手动调整：** 在每条服务项明细提供“上移/下移”，可按业务执行顺序调整并保存。
- **顺序调整动画反馈：** 明细重排增加平滑位移动画，清晰提示服务项的起止位置变化。
- **起运港显示口径统一：** 列表“起运港”优先显示 `portName`，兼容历史 `pol` 对象字段。
- **编辑态港口回显补齐：** 详情接口返回 `pol.portName` 时，编辑弹窗会手动拼装 `selected-items`，并统一 `polId` 为字符串口径，避免分页下拉未命中或超大整型 ID 类型不一致导致起运港不显示。
- **服务项类型枚举稳态回显：** 列表优先使用后端枚举文案字段，并在编辑回填时做数值归一，避免字符串/数字混用导致不回显。
- **子表差异更新：** 明细中的展示字段、锁定字段、必填字段均按“有 id 更新、无 id 新增、缺失删除”规则提交。
- **枚举驱动：** 弹窗会同时拉取 `serviceType` 与 `SeaExportPropEnum` 两类枚举，分别用于服务项类型下拉和字段规则下拉。

# 3. 状态流转说明 (Status Transitions)

| 当前状态 | 触发人/动作 | 目标状态 | 状态说明 |
| :-- | :-- | :-- | :-- |
| 未配置 | 基础数据管理员新增起运港配置 | 已配置 | 生成一条主配置及多条服务项明细。 |
| 已配置 | 基础数据管理员编辑配置 | 已配置（更新） | 明细及其子表按差异规则增删改。 |
| 已配置 | 基础数据管理员删除配置 | 已删除 | 主表及子表级联删除。 |

# 4. 核心字段说明 (Field Definitions)

| 字段名 | 📖 字段含义说明 | 🔌 数据来源 (接口/字典) | 🔗 联动规则 (依赖与触发) | 🛡️ 校验限制 (Validation) |
| :-- | :-- | :-- | :-- | :-- |
| **polId** | 配置所属起运港。 | `PortCode` 基础数据<br/>`PortSelect` | 决定配置作用范围；同租户内唯一。 | 必填；后端校验港口存在且不能重复配置。 |
| **serviceType** | 服务项类型。 | 系统枚举<br/>`serviceType` | 同一配置下服务项展示顺序由数组顺序确定。 | 同一配置下不可重复。 |
| **userAttribute** | 服务项责任角色（Flags）。 | 用户属性枚举 | 一个服务项可绑定多个角色。 | 位标志可组合，提交为整型掩码。 |
| **seServiceShows / Locks / Requires** | 字段展示、锁定、必填规则。 | 系统枚举<br/>`SeaExportPropEnum` | 与服务项绑定，随服务项一起差异更新。 | 枚举值应为有效海运出口字段。 |

# 5. 核心业务卡点 (Business Blockers)

> [!IMPORTANT] **[卡点 1：编辑排序口径]** 前端需保证 `seServiceConfigItems` 数组顺序与每项 `sortId`（从 0 开始）一致提交；若仅调整其一，可能引发回显顺序漂移。

# 6. 变更与解析日志 (Changelog & Insights)

| 日期 | 变更类型 | 📝 业务功能变动 (针对工作流A) | 🤖 代码解析与架构洞察 (针对工作流B) |
| :-- | :-- | :-- | :-- |
| 2026-05-21 | `Fix` | 修复港口服务项配置编辑态起运港回显：详情返回 `pol.portName` 时，手动注入 `PortSelect` 的 `selected-items`，并统一 `polId` 字符串口径，确保下拉稳定显示已选港口。 | 分页下拉在编辑场景需同时具备 `value + selected item object`；同时超大整型 ID 需规避 `number/string` 混用，否则会出现“有值不显示”的匹配失败。 |
| 2026-05-21 | `Feature` | 服务项“上移/下移”增加重排位移动画，用户可直观看到服务项从原位置移动到目标位置。 | 使用 `TransitionGroup + stable key` 实现 FLIP 过渡，并补充 `prefers-reduced-motion` 兼容分支。 |
| 2026-05-20 | `Feature` | 服务项配置明细新增“上移/下移”手动排序能力，保存时按当前顺序写入 `sortId`，编辑回显按 `sortId` 排序。 | 排序口径从“仅依赖数组顺序”升级为“数组顺序 + sortId 双一致”，降低前后端排序歧义。 |
| 2026-05-20 | `Fix` | 修正列表“起运港”回显口径为 `portName`，并补齐服务项类型在列表/编辑场景的枚举稳定回显。 | 列表采用“后端文案优先 + 前端映射兜底”策略，编辑态对 `serviceType` 做数值归一，降低 DTO 类型漂移对 UI 的影响。 |
| 2026-05-20 | `Fix` | 服务项配置明细移除内层纵向滚动，统一使用外层弹窗滚动，消除双滚动条。 | 删除内层 `max-height + overflow` 后滚动链路更单一，滚轮与触控滚动交互更稳定。 |
| 2026-05-20 | `Fix` | 服务项标题改为左侧品牌蓝竖线样式，去除浅底色和圆角；同时进一步收窄弹窗宽度。 | 通过“色条 + 字重”维持标题识别度，减少块状底色对表单阅读路径的干扰。 |
| 2026-05-20 | `Fix` | 服务项配置弹窗整体宽度收窄，并增强“服务项 #序号 · 服务项名称”标题样式，提升阅读聚焦。 | 仅涉及容器宽度和标题视觉样式，不影响字段绑定、校验与提交流程。 |
| 2026-05-20 | `Fix` | 服务项明细标题由“服务项 #序号”增强为“服务项 #序号 · 服务项名称”，提升多条编辑时的识别效率。 | 标题文案复用 `serviceType` 下拉枚举 `label`，无需新增映射表，兼容现有枚举加载链路。 |
| 2026-05-20 | `Fix` | 将“自动完成 / 允许手动完成 / 完成提醒”改为 `Switch` 并放在同一行展示，优化弹窗空间利用率。 | 该三项均为布尔语义字段，切换为 `Switch` 不影响 DTO 映射与提交流程。 |
| 2026-05-20 | `Fix` | 修正弹窗枚举来源：`serviceType` 与 `SeaExportPropEnum` 拆分加载，字段规则不再复用 `serviceType` 数据源。 | 明确 `serviceType` 与 `SeaExportPropEnum` 属于不同业务语义，避免字段规则下拉项被服务项类型枚举污染。 |
| 2026-05-20 | `Feature` | 新增基础数据菜单“海运出口港口服务项配置”，完成列表、弹窗增删改、子表差异更新与 `serviceType` 枚举动态加载。 | 页面采用“列表项平铺 + 配置级编辑”模式：列表显示 `SeServiceConfigItem`，操作时使用 `seServiceConfigId` 打开配置级详情与保存。 |
