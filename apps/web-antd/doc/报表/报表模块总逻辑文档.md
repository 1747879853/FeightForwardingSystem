---
title: 报表模块总逻辑文档
module: 报表(利润报表 / 欠费报表)
author: 待填写
last_updated: 2026-08-19
---

# 0. 文档说明

本文档是**报表模块的唯一逻辑口径来源**，供开发、测试、前端共同使用。文档落地了《原始逻辑文档.txt》中已确认的全部逻辑，并把口径拆到字段级。

本模块包含 3 个新增接口 + 1 个存量表字段新增：

| 序号 | 内容                                  | 类型       |
| :--- | :------------------------------------ | :--------- |
| 1    | 利润报表列表                          | 新增接口   |
| 2    | 欠费报表列表                          | 新增接口   |
| 3    | 海空港口合并分页列表                  | 新增接口   |
| 4    | `TransportOrder` 新增「结算方式」字段 | 存量表变更 |

---

# 1. 业务背景说明 (Background)

**白话解释：**

公司接一票货（一条业务），围绕这票货会产生一堆收付费用：向委托单位收运费（应收），向船公司/场站/代理付费（应付）。业务开始后如果发生变更（改港、改船期、加费用等），会开「更改单」，更改单上也能挂自己的费用。

- **利润报表**：老板视角。一票（或一票的某个更改单）赚了多少钱 = 应收 − 应付，以及利润率。看的是**全部审核通过的费用**，不分收付。
- **欠费报表**：财务视角。哪些钱该收还没收到（或该付还没付出去），欠了多久。按**结算对象**（欠谁的钱 / 谁欠我们的钱）拆开看。

两个报表都是**查询 `OrderFee`（业务费用表）后按维度聚合**，不是查业务表。

---

# 2. 数据模型基础 (测试必读)

## 2.1 表关系

```
TransportOrder (业务主表 App_TransportOrders)
  ├─ SeaExport (海运出口 App_SeaExports)     BizType = 0，与主表共用同一个 Id
  ├─ SeaImport (海运进口 App_SeaImports)     BizType = 1，与主表共用同一个 Id
  ├─ AirExport (空运出口 App_AirExports)     BizType = 2，与主表共用同一个 Id
  ├─ ChangeOrder[]   (更改单 App_ChangeOrders)
  ├─ OrderFee[]      (费用 App_OrderFees)      ← 报表的数据源
  ├─ OrderCtn[]      (箱型箱量 App_OrderCtns)
  ├─ OrderUser[]     (业务干系人 App_OrderUsers)
  └─ OrderCodeGoods[](品名 App_OrderCodeGoodss)
```

**关键点：**

1. 费用只有 `TransportOrderId`（必填）和 `ChangeOrderId`（可空），**没有业务类型字段**。业务类型要 join 主表的 `BizType` 才知道。
2. `ChangeOrderId` 为空 = **原票费用**；不为空 = 该**更改单的费用**。系统里没有「是否原票」这一列，全靠 `ChangeOrderId` 是否为空判断。
3. 费用自身**不参与数据权限**（没有 `UserId`/`OrgId`），所属组织统一取主表 `TransportOrder.OrgId`。

## 2.2 三种业务类型的字段差异（重要，直接影响筛选结果）

下表中标 ✗ 的表示该业务类型**根本没有这个字段**。

| 字段 | 存放位置 | 海运出口 | 海运进口 | 空运出口 |
| :-- | :-- | :-: | :-: | :-: |
| 委托单位 | `TransportOrder.ClientId` | ✓ | ✓ | ✓ |
| 委托编号 | `TransportOrder.CommissionNum` | ✓ | ✓ | ✓ |
| 主提单号 | `TransportOrder.MblNum` | ✓ | ✓ | ✓ |
| 业务日期 | `TransportOrder.ETD` | ✓ 开船日期 | ✓ **到港日期** | ✓ 起飞日期 |
| 会计期间 | `TransportOrder.AccountDate` / `ChangeOrder.AccountDate` | ✓ | ✓ | ✓ |
| 货物类型 | `TransportOrder.CargoId` | ✓ | ✓ | ✓ |
| 件数/重量/尺码 | `TransportOrder.PKGS/Kgs/Cbm` | ✓ | ✓ | ✓ |
| 所属组织 | `TransportOrder.OrgId` | ✓ | ✓ | ✓ |
| 干系人 | `OrderUser` | ✓ | ✓ | ✓ |
| 品名 | `OrderCodeGoods` | ✓ | ✓ | ✓ |
| 订舱代理 | `SeaExport/AirExport.BookingAgentId` | ✓ | **✗** | ✓ |
| 船公司 | `SeaExport/SeaImport.CarrierId` | ✓ | ✓ | **✗** |
| 场站 | `SeaExport.YardId` | ✓ | **✗** | **✗** |
| 船名 | `SeaExport/SeaImport.Vessel` | ✓ | ✓ | **✗** |
| 航次 | `SeaExport/SeaImport.InnerVoyno` | ✓ | ✓ | **✗** |
| 装运方式 | `SeaExport.BLType` | ✓ | **✗** | **✗** |
| 起运港/目的港 | `POLId`/`PODId` | 海运港 `PortCode` | 海运港 `PortCode` | **空运港 `AirPort`** |
| 箱型箱量 | `OrderCtn` | ✓ | ✓ | **✗ 空运无集装箱** |

> [!IMPORTANT] **规则：查询条件中的字段在某业务类型上不存在时，该业务类型的数据直接排除。**
>
> 例：填了「船公司」→ 空运出口的所有数据都不会出现在结果里；填了「装运方式」或「场站」→ 只剩海运出口的数据。
>
> 返回字段同理：空运出口行的「船公司/船名/航次/装运方式/场站/箱型箱量」返回 null 或空列表。

## 2.3 港口的两套 Id 空间

海运港（`App_PortCodes`）和空运港（`App_AirPorts`）是两张独立的表，主键都是 `long`，**id 会重复且互不相干**。

因此起运港/目的港的查询条件**必须成对传**：`港口Id` + `是否海运港`。只传 id 会匹配到错误的港口。

为此新增「海空港口合并分页列表」接口（见第 7 章），前端从该接口选港口，拿到的每一项都自带 `IsSeaPort` 标记，原样回传即可。

---

# 3. 通用规则 (两个报表共用)

## 3.1 过滤层级 (核心，决定金额算得对不对)

所有查询条件按作用对象分三层，**过滤时机不同**：

| 层级 | 作用对象 | 包含的条件 | 不满足时的效果 |
| :-- | :-- | :-- | :-- |
| **L1 业务级** | `TransportOrder` + 对应扩展表 | 业务类型、委托单位、订舱代理、船公司、场站、业务日期起/止、主提单号、委托编号、起运港、目的港、船名、航次、货物类型、装运方式、结算方式、所属组织、销售/操作/客服/单证/航线/海外客服 | 该业务下**所有**费用都不参与 |
| **L2 单据级** | `TransportOrder` 或 `ChangeOrder`（看费用挂在哪） | 会计期间起/止、是否费用锁定 | 挂在该单据下的费用不参与 |
| **L3 费用级** | `OrderFee` | 审核状态、收付类型、结算状态、付费申请状态、开票状态、是否对账 | 该条费用不参与 |

**L2 的判定方式，按是否合并更改单分两种：**

- **合并 = 否**：原票费用（`ChangeOrderId` 为空）取 `TransportOrder` 上的值；更改单费用取**它自己所属的那张 `ChangeOrder`** 上的值。
- **合并 = 是**：**只按 `TransportOrder` 判定**。主单命中则该业务的全部费用（含所有更改单的）都参与；主单不命中则整个业务排除。

合并模式之所以不逐单据判定，是为了让「筛出来的」和「显示的」是同一个值（显示口径见 3.3）。代价是合并时更改单自己的会计期间和锁定状态筛不到，只有不合并时才筛得到。

**执行顺序：** L1 → L2 → L3 逐层过滤费用行 → 剩下的费用行按第 3.2 节分组 → **过滤后一条费用都不剩的组，整组不返回**。

> [!IMPORTANT] **测试要点：** 组内的金额只统计**过滤后保留下来的费用行**。
>
> 例：某业务对客户 A 有 3 条应收，2 条未结算、1 条已结算完毕。筛「结算状态 = 未结算」时，这一行的「应收」只有那 2 条之和，**不是这票对 A 的全部应收**。这是设计如此，不是 bug。

## 3.2 分组规则

| 报表 | 是否合并更改单 = 否 | 是否合并更改单 = 是 |
| :-- | :-- | :-- |
| 利润报表 | 按 `主单Id + 更改单Id` 分组 | 按 `主单Id` 分组 |
| 欠费报表 | 按 `主单Id + 更改单Id + 结算对象Id` 分组 | 按 `主单Id + 结算对象Id` 分组 |

**说明：**

- 「否」时更改单Id 为空的费用自成一组（即原票组），每个更改单各自一组。
- 「是」时原票和所有更改单的费用合并进同一组。
- 欠费报表的**结算对象为空**（费用处于录入状态时允许为空）的费用，**单独归为一个「空结算对象」组**，不排除。

## 3.3 合并模式下组内不唯一字段的取值

合并（是否合并更改单 = 是）时，一个组里同时有原票和更改单的费用。以下字段固定取**原票（主单）**的值，与 3.1 中 L2 条件的判定口径保持一致：

| 字段                   | 合并 = 是 时取值             |
| :--------------------- | :--------------------------- |
| 是否原票 / 更改单-原票 | 固定返回「原票」             |
| 会计期间               | `TransportOrder.AccountDate` |
| 是否费用锁定           | `TransportOrder.FeeLocked`   |

不合并时无歧义：原票组取 `TransportOrder` 的值，更改单组取该 `ChangeOrder` 自己的值。

> [!NOTE] 「是否原票」在合并模式下**没有区分意义**（组里本来就混着原票和更改单的费用），恒返回「原票」。极端情况下该业务的费用可能全部挂在更改单上、组内一条原票费用都没有，此时仍然显示「原票」，属于预期行为。

## 3.4 数据权限（三层，全部要过）

能不能看到一条费用，取决于能不能看到它对应的**业务数据**。三层全部通过才返回：

1. **行级数据权限**：按业务类型分别走 `SeaExportListDataPermissionFilterAsync` / `SeaImportListDataPermissionFilterAsync` / `AirExportListDataPermissionFilterAsync`。判定口径为：数据所属人、创建人、所属组织（含下属组织）、业务干系人（销售/商务/操作/客服/单证/海外客服/航线）任一命中即可见。

   > 这三个方法原先是海出/海进/空出各自 `AdminAppService` 的 `private` 方法，报表调不到。本次统一上提到 `CsprojBuilderAppServiceBase`：泛型实现是 `BizOrderListDataPermissionFilterAsync<T>`（约束 `T : DataPermissionFullAuditedEntity<Guid>`，三张扩展表与主单共用同一个 Id，所以干系人子查询可以直接拿主单id和扩展表Id比），三个具名方法作为薄包装保留，原有调用点未改动。**三个 AppService 里的私有实现必须删除**，否则会隐藏基类方法。以后再加业务类型，直接调泛型方法即可。

2. **表级权限**：`TablePermissionsAsync(query, FrightModule.SeaExport / SeaImport / AirExport)`。
3. **机密费用权限**：没有 `Admin.OrderFee.Confidential.Get` 权限的用户，`IsConfidential = true` 的费用**不参与统计**（连带影响该组的金额与合计）。

## 3.5 所属组织

- **作为查询条件**：传一个组织 id，实际搜索「该组织 + 其所有下属组织」。
- **作为返回字段**：返回**从最高级组织到本组织的一串** `List<OrganizationUnitSimpleDto>`。取值来源是 `TransportOrder.OrgId`；`OrgId` 为空时返回空列表。

## 3.6 汇率与合计

- 折算汇率一律取 **`OrderFee.ExchangeRate`（该费用自己的汇率）**，不重新按期间取汇率表。
- 折算方向：**本位币(人民币)金额 = 原币金额 × ExchangeRate**。
- 应收和应付在汇率表里取的是不同的值（应收 `DRValue`、应付 `CRValue`），但报表不关心，直接用费用行上已存好的 `ExchangeRate`。
- **汇率为 0 时按 0 计算**（该费用折算后计入 0，不报错、不跳过）。因此合计金额可能小于原币明细之和。
- 分母为 0 导致算不出来的比率类字段，一律**返回 null**（不返回 0，前端显示为空）。

## 3.7 分页与排序

**两个报表都不分页**，一次性返回全部结果 `List<T>`。

查询条件**不做必填兜底**：业务日期、会计期间均为选填，允许不带任何条件做全量查询。

> [!NOTE] 金额聚合是在数据库端按「业务 + 更改单 + (结算对象) + 币别 + 收付」分组求和后才取回的，不会把命中的费用行整行拉进内存。发票号也是直接 join 费用查询、在数据库端投影到分组粒度。数据量涨上来之后的进一步优化路线（强制时间范围、覆盖索引、列存索引、索引视图、只读副本、独立 OLAP 库）见同目录《性能达到瓶颈后的优化方案.md》。

默认排序：

| 报表     | 默认排序                   |
| :------- | :------------------------- |
| 利润报表 | 业务日期倒序，委托编号倒序 |
| 欠费报表 | 超期天数倒序，业务日期倒序 |

---

# 4. 利润报表

## 4.1 接口

| 项       | 值                                                            |
| :------- | :------------------------------------------------------------ |
| 接口地址 | `POST /api/services/app/ReportAdmin/GetProfitReportListAsync` |
| 方法名   | `GetProfitReportListAsync`                                    |
| 入参     | `ProfitReportQueryDto`                                        |
| 出参     | `List<ProfitReportDto>`                                       |
| 权限     | `Admin.Report.Profit.Get`                                     |

## 4.2 固定过滤条件（不由前端传）

- **只统计审核通过的费用**：`OrderFee.FeeStatus = Passed(2)`。录入、提交审核、驳回状态的费用一律不参与。

## 4.3 输入参数 `ProfitReportQueryDto`

> 未标「必填」的均为选填；未标「多选」的均为单选。

| 字段名 | 类型 | 含义 | 层级 | 说明 |
| :-- | :-- | :-- | :-: | :-- |
| `IsMergeChangeOrder` | `bool` | 是否合并更改单 | — | **必填**，见 3.2 |
| `BizType` | `BizType?` | 业务类型 | L1 | 0海运出口 1海运进口 2空运出口 |
| `ClientId` | `Guid?` | 委托单位 | L1 | `TransportOrder.ClientId` |
| `BookingAgentId` | `Guid?` | 订舱代理 | L1 | 海运进口无此字段，会被排除 |
| `CarrierId` | `long?` | 船公司 | L1 | 空运出口无此字段，会被排除 |
| `YardId` | `Guid?` | 场站 | L1 | 仅海运出口有 |
| `BizDateStart` | `DateTime?` | 业务日期起 | L1 | 以天为单位，含当天 |
| `BizDateEnd` | `DateTime?` | 业务日期止 | L1 | 以天为单位，含当天 |
| `AccountDateStart` | `DateTime?` | 会计期间起 | L2 | 以月为单位，含当月 |
| `AccountDateEnd` | `DateTime?` | 会计期间止 | L2 | 以月为单位，含当月 |
| `MblNum` | `string` | 主提单号 | L1 | 模糊匹配 |
| `CommissionNum` | `string` | 委托编号 | L1 | 模糊匹配 |
| `POLId` | `long?` | 起运港Id | L1 | 必须与 `POLIsSeaPort` 同时传 |
| `POLIsSeaPort` | `bool?` | 起运港是否海运港 | L1 | true 查 `PortCode`，false 查 `AirPort` |
| `PODId` | `long?` | 目的港Id | L1 | 必须与 `PODIsSeaPort` 同时传 |
| `PODIsSeaPort` | `bool?` | 目的港是否海运港 | L1 | 同上 |
| `Vessel` | `string` | 船名 | L1 | 模糊匹配，空运出口无此字段 |
| `InnerVoyno` | `string` | 航次 | L1 | 模糊匹配，空运出口无此字段 |
| `CargoId` | `CargoType?` | 货物类型 | L1 | 0普通货 1冻柜 2危险品 3超限箱 |
| `BLType` | `BLType?` | 装运方式 | L1 | 0整箱 1拼箱主票 2拼箱分票；仅海运出口有 |
| `SettlementType` | `SettlementType?` | 结算方式 | L1 | 0票结 1月结 2约定天数；见第 8 章 |
| `OrgId` | `long?` | 所属组织 | L1 | 含所有下属组织 |
| `SaleUserIds` | `List<long>` | 销售 | L1 | **多选**，`UserAttribute.Sale(16)` |
| `OperationUserIds` | `List<long>` | 操作 | L1 | **多选**，`Operation(1)` |
| `CustomerServiceUserIds` | `List<long>` | 客服 | L1 | **多选**，`CustomerService(2)` |
| `DocumentationUserIds` | `List<long>` | 单证 | L1 | **多选**，`Documentation(4)` |
| `LanerUserIds` | `List<long>` | 航线 | L1 | **多选**，`Laner(256)`，指干系人不是港口航线 |
| `OverseasCustomerServiceUserIds` | `List<long>` | 海外客服 | L1 | **多选**，`OverseasCustomerService(64)` |

**多选人员条件的判定：** 同一类人员内多选为「或」（命中任一即可）；不同类人员之间为「且」。

## 4.4 输出字段 `ProfitReportDto`（一组一行）

### 业务信息

| 字段名 | 类型 | 含义 | 数据来源 |
| :-- | :-- | :-- | :-- |
| `TransportOrderId` | `Guid` | 业务Id | 分组键，供前端跳转 |
| `ChangeOrderId` | `Guid?` | 更改单Id | 分组键；合并模式恒为 null |
| `BizType` | `BizType` | 业务类型 | `TransportOrder.BizType` |
| `IsOriginal` | `bool` | 是否原票 | `ChangeOrderId == null`；合并模式恒为 true |
| `Client` | `ClientSimpleDto` | 委托单位 | `TransportOrder.ClientId` |
| `BookingAgent` | `ClientSimpleDto` | 订舱代理 | 海运进口为 null |
| `Carrier` | `CarrierSimpleDto` | 船公司 | 空运出口为 null |
| `Yard` | `ClientSimpleDto` | 场站 | 仅海运出口有值 |
| `BizDate` | `DateTime?` | 业务日期 | `TransportOrder.ETD` |
| `AccountDate` | `DateTime` | 会计期间 | 见 3.3 |
| `MblNum` | `string` | 主提单号 | `TransportOrder.MblNum` |
| `CommissionNum` | `string` | 委托编号 | `TransportOrder.CommissionNum` |
| `POL` | `SeaAirPortSimpleDto` | 起运港 | 海运取 `PortCode`，空运取 `AirPort` |
| `POD` | `SeaAirPortSimpleDto` | 目的港 | 同上 |
| `Vessel` | `string` | 船名 | 空运出口为 null |
| `InnerVoyno` | `string` | 航次 | 空运出口为 null |
| `Ctns` | `List<ReportCtnSimpleDto>` | 箱型箱量 | 空运出口返回空列表 |
| `CargoId` | `CargoType` | 货物类型 | `TransportOrder.CargoId` |
| `BLType` | `BLType?` | 装运方式 | 仅海运出口有值 |
| `SettlementType` | `SettlementType?` | 结算方式 | `TransportOrder.SettlementType` |
| `Orgs` | `List<OrganizationUnitSimpleDto>` | 所属组织串 | 见 3.5 |
| `Sales` | `List<UserSimpleDto>` | 销售 | `OrderUser` 按属性筛 |
| `Operations` | `List<UserSimpleDto>` | 操作 | 同上 |
| `CustomerServices` | `List<UserSimpleDto>` | 客服 | 同上 |
| `Documentations` | `List<UserSimpleDto>` | 单证 | 同上 |
| `Laners` | `List<UserSimpleDto>` | 航线 | 同上 |
| `OverseasCustomerServices` | `List<UserSimpleDto>` | 海外客服 | 同上 |
| `CodeGoodss` | `List<CodeGoodsSimpleDto>` | 品名 | `OrderCodeGoods` |
| `PKGS` | `int?` | 件数 | `TransportOrder.PKGS` |
| `Kgs` | `decimal?` | 重量 | `TransportOrder.Kgs` |
| `Cbm` | `decimal?` | 尺码 | `TransportOrder.Cbm` |

### 金额（币别明细 + 合计）

| 字段名            | 类型                      | 含义                 |
| :---------------- | :------------------------ | :------------------- |
| `Currencies`      | `List<ProfitCurrencyDto>` | 按币别分组的原币明细 |
| `TotalReceivable` | `decimal`                 | 合计应收（本位币）   |
| `TotalUnReceived` | `decimal`                 | 合计未收（本位币）   |
| `TotalPayable`    | `decimal`                 | 合计应付（本位币）   |
| `TotalUnPaid`     | `decimal`                 | 合计未付（本位币）   |
| `TotalProfit`     | `decimal`                 | 合计利润（本位币）   |
| `TotalProfitRate` | `decimal?`                | 合计利润率           |

**`ProfitCurrencyDto`（币别明细，原币，不折算）：**

| 字段名 | 类型 | 含义 | 计算公式 |
| :-- | :-- | :-- | :-- |
| `Currency` | `CurrencySimpleDto` | 币别 | — |
| `Receivable` | `decimal` | 应收 | `Σ Amount (PaySide=收)` |
| `UnReceived` | `decimal` | 未收 | `Σ (Amount − SettledAmount) (PaySide=收)` |
| `Payable` | `decimal` | 应付 | `Σ Amount (PaySide=付)` |
| `UnPaid` | `decimal` | 未付 | `Σ (Amount − SettledAmount) (PaySide=付)` |
| `Profit` | `decimal` | 利润 | `Receivable − Payable` |

**合计的计算公式（全部折算到本位币人民币）：**

```
TotalReceivable = Σ (Amount × ExchangeRate)                    其中 PaySide = 收
TotalUnReceived = Σ ((Amount − SettledAmount) × ExchangeRate)  其中 PaySide = 收
TotalPayable    = Σ (Amount × ExchangeRate)                    其中 PaySide = 付
TotalUnPaid     = Σ ((Amount − SettledAmount) × ExchangeRate)  其中 PaySide = 付
TotalProfit     = TotalReceivable − TotalPayable
TotalProfitRate = TotalPayable == 0 ? null : TotalProfit / TotalPayable
```

> [!IMPORTANT] **利润率的分母是「应付」，不是「应收」**，这是本项目确认的口径。应付为 0 时返回 null。

---

# 5. 欠费报表

## 5.1 接口

| 项 | 值 |
| :-- | :-- |
| 接口地址 | `POST /api/services/app/ReportAdmin/GetArrearsReportListAsync` |
| 方法名 | `GetArrearsReportListAsync` |
| 入参 | `ArrearsReportQueryDto` |
| 出参 | `List<ArrearsReportDto>` |
| 权限 | 收付类型=收 → `Admin.Report.Arrears.Receive.Get`；收付类型=付 → `Admin.Report.Arrears.Pay.Get` |

## 5.2 固定过滤条件

- **不筛选费用的审核状态**：录入、提交审核、审核通过、驳回的费用全部参与。

## 5.3 输入参数 `ArrearsReportQueryDto`

除下表列出的**新增/差异**条件外，其余条件与利润报表 4.3 节**完全一致**（业务类型、委托单位、订舱代理、船公司、场站、业务日期起止、会计期间起止、主提单号、委托编号、起运港、目的港、船名、航次、货物类型、装运方式、结算方式、所属组织、六类人员多选）。

| 字段名 | 类型 | 含义 | 层级 | 说明 |
| :-- | :-- | :-- | :-: | :-- |
| `PaySide` | `PaySide` | 收付类型 | L3 | **必填**，0收 1付。决定权限与字段语义 |
| `IsMergeChangeOrder` | `bool` | 是否合并更改单 | — | **必填** |
| `SettlementId` | `Guid?` | 结算对象 | L3 | `OrderFee.SettlementId`，即欠谁/谁欠 |
| `SettlementStatus` | `SettlementStatus?` | 结算状态 | L3 | 0未结算 1部分结算 2结算完毕 |
| `PaymentApplyStatus` | `FeePaymentApplyStatus?` | 付费申请状态 | L3 | 见下方判定口径 |
| `InvoiceStatus` | `InvoiceStatus?` | 开票状态 | L3 | 0未开票 1部分开票 2已开票；收付都正常生效 |
| `IsStatemented` | `bool?` | 是否对账 | L3 | 存在未删除的 `StatementFee` 即为已对账 |
| `FeeLocked` | `bool?` | 是否费用锁定 | L2 | 见 3.1 的 L2 判定 |

**`FeePaymentApplyStatus`（付费申请状态）判定口径：**

只看该费用被付费申请「提交」了多少金额（`Σ PaymentApplicationItem.AppliedAmount`），**不看付费申请单本身处于什么状态**（驳回的申请明细同样算已提交）。系统不存在超额提交。

注意与 `PaymentApplicationStatus`（付费申请单自己的审批状态：录入/审核中/驳回/通过/部分结算/结算完毕）不是一回事。

| 枚举值 | 含义 | 判定 |
| :-- | :-- | :-- |
| `NotSubmitted = 0` | 未提交 | 没有任何付费申请明细 |
| `PartiallySubmitted = 1` | 提交了部分 | 有申请明细，且 `Σ AppliedAmount != OrderFee.Amount` |
| `FullySubmitted = 2` | 已全额提交 | 有申请明细，且 `Σ AppliedAmount == OrderFee.Amount` |

收付类型选「收」时该条件照常生效（结果自然为空，不做置灰或忽略处理）。

## 5.4 输出字段 `ArrearsReportDto`（一组一行）

### 业务信息

与利润报表 4.4 节相同的字段：`TransportOrderId`、`ChangeOrderId`、`BizType`、`IsOriginal`、`Client`、`BookingAgent`、`Carrier`、`Yard`、`BizDate`、`AccountDate`、`MblNum`、`CommissionNum`、`POL`、`POD`、`Vessel`、`InnerVoyno`、`Ctns`、`CargoId`、`BLType`、`SettlementType`、`Orgs`、`Sales`、`Operations`、`CustomerServices`、`Documentations`、`Laners`、`OverseasCustomerServices`、`CodeGoodss`、`PKGS`、`Kgs`、`Cbm`。

### 欠费报表专有字段

| 字段名 | 类型 | 含义 | 数据来源 / 计算公式 |
| :-- | :-- | :-- | :-- |
| `Settlement` | `ClientSimpleDto` | 结算对象 | `OrderFee.SettlementId`；空结算对象组返回 null |
| `FeeLocked` | `bool` | 费用锁定 | 见 3.3 |
| `SettlementDate` | `DateTime` | 应结日期 | `TransportOrder.SettlementDate` |
| `OverdueDays` | `int` | 超期天数 | `(今天.Date − 应结日期.Date).Days`，舍掉小时；**未到期返回负数** |
| `InvoiceNos` | `List<string>` | 发票号 | 见 5.5 |
| `Currencies` | `List<ArrearsCurrencyDto>` | 按币别分组的原币明细 | — |
| `TotalReceivable` | `decimal` | 合计应收/应付 | `Σ (Amount × ExchangeRate)` |
| `TotalReceived` | `decimal` | 合计已收/已付 | `Σ (SettledAmount × ExchangeRate)` |
| `TotalUnReceived` | `decimal` | 合计未收/未付 | `TotalReceivable − TotalReceived` |

**`ArrearsCurrencyDto`（币别明细，原币，不折算）：**

| 字段名 | 类型 | 含义 | 计算公式 |
| :-- | :-- | :-- | :-- |
| `Currency` | `CurrencySimpleDto` | 币别 | — |
| `Receivable` | `decimal` | 应收/应付 | `Σ Amount` |
| `Received` | `decimal` | 已收/已付 | `Σ SettledAmount` |
| `UnReceived` | `decimal` | 未收/未付 | `Σ (Amount − SettledAmount)` |

> [!NOTE] 因为收付类型是必填的费用级条件，一个组里只会有单一方向的费用。收付类型 = 收 时这三个字段读作「应收/已收/未收」，= 付 时读作「应付/已付/未付」，**字段名复用同一套**。

## 5.5 发票号的取数逻辑

收付两个方向走**完全不同**的链路：

**应收（`PaySide = 收`）：**

```
OrderFee → InvoiceApplicationItem → InvoiceApplication → InvoiceIssueItem → InvoiceIssue.InvoiceNo
```

- 直接复用已有的 `IOrderFeeInvoicedProvider.GetInvoiceMapAsync(orderFeeIds)`，取其中的 `InvoiceNo`。
- 权威来源是 `InvoiceIssue.InvoiceNo`（真正开出的票），**不取** `InvoiceApplication.InvoiceNo`（那是冗余副本）。
- **发票冲红完成后挂靠会被自动解除**，该票不会再出现在结果里，无需额外处理红冲状态。
- 一条费用可以拆到多张发票上，所以是列表。

**应付（`PaySide = 付`）：**

```
OrderFee → PaymentApplicationItem → PaymentApplication.InvoiceNo
```

- 系统中没有现成的批量解析器，需自行 join。
- 与 5.3 节「付费申请状态只看是否提交」的口径保持一致，**不按付费申请单状态过滤**，取所有 `InvoiceNo` 非空的记录。
- 同一费用可能出现在多张付费申请上，所以是列表。

组内多条费用的发票号**去重合并**后返回。

---

# 6. 公共 DTO 定义

## 6.1 `SeaAirPortSimpleDto`（港口，海空统一）

定义在 `App\PortCode\Dto\PortCodeDto.cs`，报表与港口列表接口共用。

| 字段名      | 类型     | 含义                                           |
| :---------- | :------- | :--------------------------------------------- |
| `Id`        | `long`   | 港口Id                                         |
| `IsSeaPort` | `bool`   | 是否海运港。true=`PortCode`，false=`AirPort`   |
| `CnName`    | `string` | 中文名                                         |
| `EnName`    | `string` | 英文名。海运港取 `PortName`，空运港取 `EnName` |
| `Code`      | `string` | 代码。海运港取 `EdiCode`，空运港取 `IataCode`  |

## 6.2 `ReportCtnSimpleDto`（箱型箱量）

| 字段名    | 类型               | 含义 |
| :-------- | :----------------- | :--- |
| `CtnCode` | `CtnCodeSimpleDto` | 箱型 |
| `Count`   | `int`              | 箱量 |

箱量 = `OrderCtn` 按 `CtnCodeId` 分组后的**行数**（系统里没有数量列，一个箱子一行）。空运出口返回空列表。

## 6.3 复用的现有 SimpleDto

| DTO | 用途 |
| :-- | :-- |
| `ClientSimpleDto` | 委托单位、订舱代理、场站、结算对象（都是 `Client` 表） |
| `CarrierSimpleDto` | 船公司 |
| `CtnCodeSimpleDto` | 箱型 |
| `CodeGoodsSimpleDto` | 品名 |
| `CurrencySimpleDto` | 币别（注意该 DTO 以 `Code` 为键，无 `Id`） |
| `UserSimpleDto` | 六类干系人，返回 `NickName` |
| `OrganizationUnitSimpleDto` | 所属组织串（用 `DataPermission.cs` 中的那个） |

---

# 7. 海空港口合并分页列表（新增接口）

## 7.1 接口

| 项       | 值                                                                |
| :------- | :---------------------------------------------------------------- |
| 接口地址 | `GET /api/services/app/PortCodeAdmin/GetSeaAirPortPagedListAsync` |
| 方法名   | `GetSeaAirPortPagedListAsync`                                     |
| 入参     | `SeaAirPortQueryDto`（继承 `Paging`）                             |
| 出参     | `PagedList<SeaAirPortSimpleDto>`                                  |

## 7.2 输入参数

| 字段名 | 类型 | 含义 |
| :-- | :-- | :-- |
| `PageIndex` | `int` | 页码，从 1 开始 |
| `PageSize` | `int` | 每页条数 |
| `Keyword` | `string` | 关键字，匹配中文名/英文名/代码 |
| `IsSeaPort` | `bool?` | 不传=海空全查；true=只查海运港；false=只查空运港 |
| `Status` | `int?` | 0启用 1禁用。不传=不过滤（报表要按历史业务搜索，可能需要已停用的港口） |

**没有 `Sorting` 参数**：输出是两张表 `Concat` 出来的投影，没有 `CreationTime` 之类的实体列，传排序字段会在运行时抛异常。排序固定为「先海运港后空运港，各自按中文名升序」。

## 7.3 输出

返回 `SeaAirPortSimpleDto`（见 6.1），即海运港 `PortCode` 与空运港 `AirPort` 的**合并集合**，数据库端 `UNION ALL` 后再分页，不会把两张表全量拉进内存。

前端在报表里选择起运港/目的港时使用此接口，选中项的 `Id` 和 `IsSeaPort` 需**成对回传**给报表查询接口。报表接口对此做了强校验：**传了港口 id 却没传 `IsSeaPort`，直接抛「传了起运港id就必须同时传起运港是否海运港」**。

---

# 8. TransportOrder 新增「结算方式」字段

## 8.1 变更内容

在 `TransportOrder`（`App_TransportOrders`）新增字段：

| 字段名           | 类型              | 含义                            |
| :--------------- | :---------------- | :------------------------------ |
| `SettlementType` | `SettlementType?` | 结算方式。0票结 1月结 2约定天数 |

## 8.2 为什么要加

结算方式原先只存在于客户账期表 `ClientBillingPeriod` 上，是按「委托单位 + 业务类型 + 销售 + 组织 + 业务来源 + 生效时间」一整套规则匹配出来的，匹配结果只用来算应结日期，算完即丢。既无法作为 SQL 查询条件，报表也返回不了。

## 8.3 赋值时机

在 `CsprojBuilderAppServiceBase.GetSettlementDateAsync` 中，**计算应结日期的同时**把命中的结算方式一并写入：

| 场景                       | `SettlementDate`        | `SettlementType` |
| :------------------------- | :---------------------- | :--------------- |
| 该客户没有任何生效账期     | 业务日期当天            | 票结             |
| 匹配不到符合条件的账期规则 | 业务日期当天            | 票结             |
| 命中规则且为票结           | 业务日期当天            | 票结             |
| 命中规则且为月结           | 按间隔月份 + 结算日计算 | 月结             |
| 命中规则且为约定天数       | 业务日期 + 约定天数     | 约定天数         |

## 8.4 语义说明（测试注意）

- 该字段是**下单当时的快照**。之后客户账期规则被修改，历史业务单上的结算方式**不会自动刷新**，与应结日期的行为一致。
- 应结日期（以及由它算出的超期天数）是按**委托单位**的账期算的。收付类型 = 付 时，应结日期同样取这个值，不按供应商账期另算。
- **存量数据不回刷**。新增列之前已存在的业务单该字段为空，按结算方式筛选时这些历史单据查不出来，属于预期行为。

## 8.5 对业务模块的暴露：可查不可改

该字段对海运出口 / 海运进口 / 空运出口三个业务模块开放，但**只读**：

| 场景 | 是否开放 | 说明 |
| :-- | :-: | :-- |
| 详情接口返回 | ✓ | `SeaExportDto` / `SeaImportDto` / `AirExportDto` 增加 `SettlementType` |
| 列表接口返回 | ✓ | 同上 |
| 作为列表查询条件 | ✓ | `SeaExportQueryDto` / `SeaImportQueryDto` / `AirExportQueryDto` 增加 `SettlementType?` 筛选 |
| 新增时录入 | **✗** | `*AddDto` **不含**该字段 |
| 修改时录入 | **✗** | `*EditDto` **不含**该字段 |

**唯一写入口**是 `GetSettlementDateAsync`（见 8.3），跟着应结日期一起自动算出来，前端无法直接改。

---

# 9. 权限点

需在 `FreightPermissions.cs` 与 `FreightAuthorizationProvider.cs` 中新增（现有的 `Admin_Reportinganalysis` 系列是注释状态，不复用）：

| 权限常量 | 权限字符串 | 说明 |
| :-- | :-- | :-- |
| `Admin_Report` | `Admin.Report` | 报表（父节点） |
| `Admin_Report_Profit_Get` | `Admin.Report.Profit.Get` | 利润报表查询 |
| `Admin_Report_Arrears_Receive_Get` | `Admin.Report.Arrears.Receive.Get` | 欠费报表-应收查询 |
| `Admin_Report_Arrears_Pay_Get` | `Admin.Report.Arrears.Pay.Get` | 欠费报表-应付查询 |

欠费报表接口在方法内部按入参 `PaySide` 判定该用哪个权限，无权限时抛出无权访问异常。

**利润报表不区分应收应付**，只用 `Admin.Report.Profit.Get` 一个权限点控制。有这个权限就能同时看到应收、应付和利润，不受欠费报表那两个权限的影响。

---

---

# 10. 代码位置与受影响文件

## 10.1 新增文件

| 文件 | 内容 |
| :-- | :-- |
| `src\CsprojBuilder.Application\App\Report\ReportAdminAppService.cs` | 两个报表接口实现 |
| `src\CsprojBuilder.Application\App\Report\IReportAdminAppService.cs` | 报表接口定义 |
| `src\CsprojBuilder.Application\App\Report\Dto\ReportDto.cs` | 报表全部 DTO |

## 10.2 修改的已有文件

| 文件 | 改了什么 |
| :-- | :-- |
| `src\CsprojBuilder.Application\CsprojBuilderAppServiceBase.cs` | 新增 `BizOrderListDataPermissionFilterAsync<T>` 及三个具名包装（见 3.4）；`GetSettlementDateAsync` 五个分支同步写入结算方式（见 8.3） |
| `src\CsprojBuilder.Application\App\SeaExport\SeaExportAdminAppService.cs` | 删除私有数据权限方法（连同其中已注释失效的服务项转交人子查询死代码）；列表查询加结算方式条件 |
| `src\CsprojBuilder.Application\App\SeaImport\SeaImportAdminAppService.cs` | 同上（无死代码） |
| `src\CsprojBuilder.Application\App\AirExport\AirExportAdminAppService.cs` | 同上（无死代码） |
| `src\CsprojBuilder.Application\App\SeaExport\Dto\SeaExportDto.cs` | 查询输入加 `SettlementType` |
| `src\CsprojBuilder.Application\App\SeaImport\Dto\SeaImportDto.cs` | 同上 |
| `src\CsprojBuilder.Application\App\AirExport\Dto\AirExportDto.cs` | 同上 |
| `src\CsprojBuilder.Application\App\TransportOrder\Dto\TransportOrderDto.cs` | `TransportOrderDto` 与 `TransportOrderSimpleDto` 加 `SettlementType` 输出 |
| `src\CsprojBuilder.Application\App\PortCode\PortCodeAdminAppService.cs` | 新增 `GetSeaAirPortPagedListAsync`；注入 `AirPort` 仓储 |
| `src\CsprojBuilder.Application\App\PortCode\IPortCodeAdminAppService.cs` | 新增接口声明 |
| `src\CsprojBuilder.Application\App\PortCode\Dto\PortCodeDto.cs` | 新增 `SeaAirPortQueryDto` / `SeaAirPortSimpleDto` |
| `src\CsprojBuilder.Core\Entites\TransportOrder.cs` | 新增 `SettlementType` 列 |
| `src\CsprojBuilder.Core\CsprojBuilderEnum.cs` | 新增 `FeePaymentApplyStatus` |
| `src\CsprojBuilder.Core\Authorization\FreightPermissions.cs` | 新增 4 个报表权限常量 |
| `src\CsprojBuilder.Core\Authorization\FreightAuthorizationProvider.cs` | 注册报表权限树 |
| `src\CsprojBuilder.Core\Localization\SourceFiles\CsprojBuilder-zh-Hans.xml` | 4 条权限汉化 |
| `src\CsprojBuilder.Application\App\InvoiceIssue\OrderFeeInvoicedProvider.cs` | 新增 `GetIssuedInvoiceNoQuery()` 不落地子查询出口，供报表把开票链路留在数据库端，避免复制口径 |

## 10.3 接口文件 ↔ 文档映射

| 接口文件 | 对应文档 |
| :-- | :-- |
| `App\Report\ReportAdminAppService.cs` | `文档\报表\报表模块总逻辑文档.md`（本文）、`文档\报表\报表模块接口文档.md` |
| `App\PortCode\PortCodeAdminAppService.cs` → `GetSeaAirPortPagedListAsync` | 本文第 7 章、接口文档第 4 章 |

## 10.4 数据库变更

`App_TransportOrders` 新增 `SettlementType`（`int` 可空）。migration 未生成，由开发本地执行。

---

# 11. 变更与解析日志 (Changelog & Insights)

| 日期 | 变更类型 | 业务功能变动 | 代码解析与架构洞察 |
| :-- | :-- | :-- | :-- |
| 2026-08-19 | `Feature` | 新增利润报表、欠费报表、海空港口合并分页列表三个接口；`TransportOrder` 新增只读的结算方式字段；新增 4 个报表权限点。 | 依据《原始逻辑文档.txt》及三轮口径确认落地。核对出的关键事实：①费用挂在 `TransportOrder` 上，无业务类型列，须 join 主表 `BizType`；②三种业务类型字段覆盖不一致（船公司/场站/订舱代理/船名/航次/装运方式各有缺失），确认「条件字段不存在则排除该业务类型」；③海运港 `PortCode` 与空运港 `AirPort` 是两套 `long` id 空间，港口条件必须带 `IsSeaPort` 成对传递，为此新增海空港口合并列表接口；④结算方式原先只在 `ClientBillingPeriod` 上按规则匹配、算完即丢，确认新增 `TransportOrder.SettlementType` 持久化列；⑤应收发票号复用 `IOrderFeeInvoicedProvider`（权威值取 `InvoiceIssue.InvoiceNo`，冲红后自动解绑），应付发票号需自行 join `PaymentApplication.InvoiceNo`，无现成解析器；⑥费用自身不参与数据权限，须经由 `SeaExport`/`SeaImport`/`AirExport` 三个列表数据权限方法反查，且这三个方法目前均为各自 AppService 的 private 方法，需上提到基类。 |
