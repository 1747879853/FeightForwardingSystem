---
title: TAPD 1000122 监装 — 执行总览
tapd: 1161580498001000122
status: 待执行
last_updated: 2026-08-23
---

# TAPD #1000122 监装：执行总览

**需求：** [监装](https://www.tapd.cn/61580498/prong/stories/view/1161580498001000122)  
**处理人：** 杨云东  
**本仓库范围：** `apps/web-antd` 前端对接。后端接口与库表已在 `D:\code\Freight` 落地。  
**本轮范围（已拍板）：只做管理端。** 师傅端小程序（认领/拒接/改箱号封号/拍照/完成）不在 01–05 验收内，见 [05](./05-监装工单.md#本仓库不做)。  
**用户属性（已拍板）：所有现有「用户属性」入口都加「监装」**，含用户管理、标签映射、海出干系人 6 项、港口服务项负责岗位、枚举管理角色下拉。见 [03](./03-用户属性监装.md)。  
**干系人 vs 工单师傅（已拍板）：两套独立，不同步。** 干系人「监装」卡与工单 `userIds` 互不回写；监装不是销售/操作那种进页必填固定角色。  
**工单下拉数据（已拍板）：只认已保存海出。** 进 Tab 用落库的包装/船公司拉明细与堆场；基础信息未保存修改不联动。见 [05](./05-监装工单.md)。  
**工单 Tab 位置（已拍板）：派车之后、分单之前。** 无 `.Get` 权限则整 Tab 隐藏。  
**列表（已拍板）：包装 / 船公司 / 监装要求都不做展开行。** 子表只在 Drawer 里看和改。  
**编号规则（已拍板）：** 编号规则页表名下拉要加 `LoadingOrder.LoadingOrderNum`。见 [05](./05-监装工单.md)。  
**已排师傅提示：** 点「推荐」弹窗调 `GetYardUsersAsync`（到货日+船公司），选中后回填堆场和师傅。接口只回名称，前端对本地 id。同一天同一船公司可以多个堆场，这不是冲突拦截。

TAPD 描述里的截图是后端 git 里「接口文档路径」指针，不是 UI 稿。UI 口径（已拍板）：**包装 / 船公司 / 监装要求 三处新建编辑都用 Drawer，对齐 `CodeGoodsAdmin`。** 子表可增删行，不要抄规格表的排序输入框。

## 需求拆条（按 TAPD 原文）

| 序号 | 计划文件 | TAPD 原文 | 工作量 | 依赖 |
| :-- | :-- | :-- | :-- | :-- |
| 1 | [01-包装明细子表.md](./01-包装明细子表.md) | 包装加明细包装子表 | 小 | 无 |
| 2 | [02-船公司堆场子表.md](./02-船公司堆场子表.md) | 船公司加堆场子表 | 小 | 无 |
| 3 | [03-用户属性监装.md](./03-用户属性监装.md) | 用户属性加枚举 `512 \| LoadingSupervision \| 监装` | 小 | 无 |
| 4 | [04-监装要求模块.md](./04-监装要求模块.md) | 基础数据新增监装要求模块 | 中 | 无 |
| 5 | [05-监装工单.md](./05-监装工单.md) | 监装真正模块 | 大 | 1–4 都要先做完 |

建议执行顺序：**3 → 1 / 2 可并行 → 4 → 5**。3 最小、独立；5 要用到明细包装、堆场、监装师傅属性、监装要求勾选。

## 后端文档根

权威仓库：`D:\code\Freight`（已 `git pull`，HEAD `6a0864b9`）。  
文档根：`D:\code\Freight\aspnet-core\文档`。

| 模块 | 前端对接文档（优先读这个） | 业务规则（补字段/状态机） |
| :-- | :-- | :-- |
| 包装明细 | `基础资料\包装类型-明细包装子表-前端接口文档-2026-08-20.md` | 同目录 `包装类型-明细包装子表-2026-08-20.md` |
| 船公司堆场 | `船公司\船公司-堆场子表-前端接口文档-2026-08-20.md` | 同目录 `船公司-堆场子表-2026-08-20.md` |
| 用户属性 | `用户\用户-用户属性新增监装-2026-08-20.md` | `用户\用户通用api.md` |
| 监装要求 | `监装\监装要求模块接口文档.md` | `监装\监装要求模块总逻辑文档.md` |
| 监装工单 | `监装\监装工单模块接口文档.md` | `监装\监装工单模块总逻辑文档.md` |

原始需求备查：`监装\原始逻辑文档.txt`。

## 共享约定（每条计划都遵守）

1. **ABP 动态 API 必须带 `Async` 后缀**，写掉会 404。
2. **子表全量提交**：保留行带 `id`、新增行 `id: null`、删除行从数组移除。漏传 = 后端删掉漏的行。
3. **子表不要传 `sortId`**：后端按数组下标生成；UI 不要做排序输入框（商品信息规格表有 `sortId` 列，**不要抄那一列**）。
4. **主表 `sortId` 仍由前端传**（包装主表、监装要求主表）。
5. **雪花 / Guid ID 禁止 `Number()`**，比较用 `String()`。
6. 落地后走 `auto-doc-sync` 写 changelog + 活文档；本计划阶段不改业务代码。

## 联调测试数据（跨会话复用）

落库清单与待建约定名：**[测试数据：TAPD 1000122 监装](../../test-data/tapd-1000122-loading-supervision.md)**。  
索引：[TEST_DATA_INDEX.md](../../test-data/TEST_DATA_INDEX.md)。

已落库：`测试纸箱` / `测试木箱`、`测试标准整箱监装要求` / `测试危险品监装要求`、正式「马士基」(MSK) 下 3 条测试堆场。  
后续建测试中远、监装师傅、海出票、工单时**必须用该文档里的名字**，不要另起一套。

## 怎么执行

对某一条回复：「执行 01」或「执行包装明细子表」。一次只做一条，做完再下一条。
