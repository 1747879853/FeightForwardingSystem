# 前端性能优化方案（apps/web-antd）

> **v2 · 2026-08-31 修订** v1 的方向判断错了。v1 按「首屏加载慢」组织方案，把字体、压缩、启动阻塞排在最前。实际反馈是 **不是加载慢，而是一直慢，且后端接口很快**。这排除了网络、包体积、API 三个方向，问题只可能出在「持续占用主线程 / GPU 的常驻开销」和「每次交互重复做的昂贵计算」。本版按这个方向重排。v1 的冷加载分析数据仍然有效，降级放到 [附录 B](#附录-b冷加载优化v1-内容降级保留)。

---

## 一、问题定性

「一直慢」+「后端快」这两个条件同时成立时，可能的原因只有几类：

| 可能原因 | 本项目是否存在 | 依据 |
| --- | --- | --- |
| 常驻的定时器 / 动画循环 | **存在** | 见 P0-1、P1-1 |
| 泄漏的全局事件监听器（越用越多） | **存在** | 见 P0-1、P0-2 |
| 监听范围过大的 Observer | **存在** | 见 P0-1 |
| 缓存页面过多导致的内存与 DOM 驻留 | **存在** | 见 P0-3 |
| 每次交互重复的大量 DOM 渲染 | **存在** | 见 P2-1 |
| Store 每次变更都加密写盘 | **不存在** | 见 [附录 A](#附录-a已排除的方向) |
| 全站无限 CSS 动画 / 大范围毛玻璃 | **不存在** | 见附录 A |
| 未节流的全局 scroll / mousemove | **不存在** | 见附录 A |

### 贯穿全文的一个根因

本项目绝大多数「常驻开销」问题都源自同一个模式错误，理解它比逐个修 bug 更重要：

**在 vben 架构下，被 keepAlive 缓存的组件永远不会 `unmount`，所以写在 `onUnmounted` / `onBeforeUnmount` 里的清理代码是死代码。**

- 全局开关：`packages/@core/preferences/src/config.ts:101` → `tabbar.keepAlive: true`
- 渲染层：`packages/effects/layouts/src/basic/content/content.vue:109` → `<KeepAlive :include="getCachedTabs">`
- 全站约 **69 条**路由带 `meta.keepAlive: true`
- 离开页面只触发 `onDeactivated`，**不触发** `onUnmounted`
- 只有标签被手动关闭、或被 `maxCount` 淘汰时才真正销毁
- 而 `tabbar.maxCount = 0`（`config.ts:102`）意味着标签**永不自动淘汰**

结果：定时器继续跑、Observer 继续监听、事件监听器持续累积，且**关掉标签页也不一定恢复**。这正好解释「一直慢」，也解释了为什么重启浏览器（而不是刷新）才感觉好一点。

详细的检查清单和修法模板见 [第四节](#四根因模式与通用修法)。

---

## 二、方案总览（按对「持续卡顿」的影响排序）

| 优先级 | 编号 | 方案 | 触发频率 | 作用范围 | 工作量 | 风险 |
| --- | --- | --- | --- | --- | --- | --- |
| **P0** | P0-1 | 修报表页三处监听器泄漏 | **全站任意 DOM 的 class/style 变化** | 打开过报表后**全站永久** | 半天 | 低 |
| **P0** | P0-2 | 修 vxe-grid 全局 click 监听器累积 | 每次点击 × 缓存的表格页数 | 全站 60+ 列表页 | 1 小时 | 低 |
| **P0** | P0-3 | keepAlive 加上限 `maxCount` | 持续 | 全站 | 10 分钟 | 低 |
| **P1** | P1-1 | 3D 桌宠改按需开启 | **每帧 ~60fps WebGL** | 仅津海通 jht，登录后全站 | 2 小时 | 低 |
| **P1** | P1-2 | 缓存页的轮询与 Observer 停在 `onDeactivated` | 30 秒 / 尺寸变化 | 海运空运编辑页等 | 半天 | 低 |
| **P2** | P2-1 | 表格虚拟滚动 | 每次打开列表 | 全站 61 个列表页 | 1–2 天 | 中高 |
| **P2** | P2-2 | 费用表 deep watch 全量重载 | 每次改动费用行 | 费用页 | 半天 | 中 |
| **P2** | P2-3 | 收敛 vxe 全局渲染开销 | 每次渲染表格 | 全站列表 | 2 小时 | 低 |
| **P3** | — | 冷加载优化（原 A / B / D） | 仅冷加载 | — | — | 见附录 B |

**关键判断**：P0 三项加起来不到一天，且都是低风险，**应该先做这三项再谈别的**。P2-1（虚拟滚动）虽然收益大，但它优化的是「打开列表的那一下」，不是「一直慢」，而且是唯一需要逐页回归的大工程，不该排在前面。

---

## 三、各方案详情

### P0-1 · 报表页三处监听器泄漏（首要嫌疑）

**文件**：`apps/web-antd/src/views/report/_shared/report-hot-table.vue`

**调用链**（已确认）：`views/report/profit-report/index.vue:2` → `_shared/report-page.vue:8` → `_shared/report-hot-table.vue`。欠费报表 `arrears-report/index.vue` 走同一条链。

**两个报表路由都开了缓存**：

```16:34:apps/web-antd/src/router/routes/modules/report.ts
      {
        path: 'profit-report',
        name: 'ProfitReport',
        meta: {
          keepAlive: true,
```

`ArrearsReport` 同样是 `keepAlive: true`（第 30 行）。

#### 问题 1：一个永远无法移除的 resize 监听器

```758:767:apps/web-antd/src/views/report/_shared/report-hot-table.vue
// 监听窗口大小变化（带防抖）
let resizeTimer: ReturnType<typeof setTimeout> | null = null;
window.addEventListener('resize', () => {
  if (resizeTimer) {
    clearTimeout(resizeTimer);
  }
  resizeTimer = setTimeout(() => {
    updateTableHeight();
  }, 100);
});
```

两个错误叠加：

1. 注册在 `<script setup>` **顶层**，不在任何生命周期钩子里 —— 组件每次实例化就注册一个。
2. 第 784 行 `window.removeEventListener('resize', updateTableHeight)` 传的是**另一个函数引用**（匿名箭头函数没有留引用），`removeEventListener` 静默失败，**永远移除不掉**。

**要做的事**：把回调提取成命名函数（如 `handleWindowResize`），`addEventListener` 移入 `onMounted`，`removeEventListener` 传同一个引用，并同时在 `onDeactivated` 里移除。

#### 问题 2：监听整个 `document.body` 的 MutationObserver

```656:659:apps/web-antd/src/views/report/_shared/report-hot-table.vue
  // 4. 使用 MutationObserver 监听 DOM 变化，动态添加新的观察对象
  let updateTimeout: ReturnType<typeof setTimeout> | null = null;

  const mutationObserver = new MutationObserver((mutations) => {
```

观察配置是 `document.body` + `subtree: true` + `attributes: true` + `attributeFilter: ['style', 'class']`。

**这意味着全站任何位置的 class 或 style 变化都会触发回调**。在 Ant Design Vue 应用里这类变化持续不断：鼠标悬停、下拉框开合、tooltip 显隐、表格行 hover、loading 状态切换、标签页切换。每批 mutation 还要跑 `mutations.forEach` 加 `node.querySelector` / `target.closest()` 的 DOM 查询（第 666–706 行）。

它的真实意图只是「查询表单展开/收缩时重算表格高度」，完全不需要监听全站。

**要做的事**：把观察目标从 `document.body` 缩小到报表页自己的容器（`.vben-page-wrapper` 或组件根 ref）；或者直接去掉 Observer，改成 `watch` 查询表单的展开状态（那是个响应式变量，比监听 DOM 可靠得多）。

#### 问题 3：ResizeObserver 也观察了 body，且清理是死代码

```642:642:apps/web-antd/src/views/report/_shared/report-hot-table.vue
  resizeObserver.observe(document.body);
```

上面三项的清理全写在 `onUnmounted`（第 782–805 行），而报表路由是 keepAlive，**这个钩子一次都不会执行**。

**要做的事**：`onUnmounted` 里的清理逻辑提取成一个 `stopObservers()`，在 `onDeactivated` 和 `onUnmounted` 里都调用；`onActivated` 里重新 `startObservers()`。

#### 性能提升

无法给出百分比（需 profiling），但可以明确影响模式：

- **修复前**：用户打开过一次利润报表或欠费报表，本次会话内**全站所有操作都变慢**，且关掉报表标签也不恢复（组件还在缓存里）。开两个报表标签就是两套 Observer。
- **修复后**：报表页的开销只在停留报表页时产生。

**这是最符合「一直慢」现象的一项，建议第一个做。**

---

### P0-2 · vxe-grid 全局 click 监听器按标签数累积

**文件**：`packages/effects/plugins/src/vxe-table/use-vxe-grid.vue`

```2169:2172:packages/effects/plugins/src/vxe-table/use-vxe-grid.vue
onMounted(() => {
  props.api?.mount?.(gridRef.value, formApi);
  window.addEventListener('click', onDocumentClick);
  void init();
});
```

移除写在 `onUnmounted`（第 2184 行）。因为列表页普遍是 keepAlive，**这个钩子不会执行**，监听器随打开的列表标签数线性累积。

全站有 **60 多个**列表页使用 `useVbenVxeGrid` 这个封装。用户开 10 个列表标签，每次点击页面任意位置就会触发 10 次 `onDocumentClick`。回调本身有早退（`if (!showSearchFieldPopover.value) return;`），单次开销很小，但这是**每次点击 × N** 的乘法关系，且和「标签开得越多越卡」的反馈一致。

**要做的事**：改成 `onActivated` 注册 / `onDeactivated` 注销（`onMounted` 和 `onUnmounted` 保留作为非缓存场景的兜底，注册函数需幂等）。

**注意**：这个文件在 `packages/effects` 里，属于框架层，改动会影响所有使用 vxe 表格的页面。改完需要验证「表格工具栏的字段搜索弹层点击外部关闭」这个功能仍然正常（那是 `onDocumentClick` 的唯一用途）。

---

### P0-3 · keepAlive 加上限

**现状**：`packages/@core/preferences/src/config.ts:102` 的 `tabbar.maxCount = 0`，标签永不自动淘汰。全站约 69 条路由带 `keepAlive: true`，主业务列表全在内。

用户一上午开 12~15 个标签，每个大列表页的 DOM、watcher、定时器、Observer 全部驻留不销毁。这是 P0-1 和 P0-2 的**放大器** —— 正因为不销毁，泄漏才会永久化。

**要做的事**：改 `apps/web-antd/src/preferences.ts` 已有的 `tabbar` 块，**不要动** `packages/@core` 的框架默认值：

```ts
tabbar: {
  middleClickToClose: true,
  showIcon: true,
  maxCount: 8,        // 新增
},
```

**注意事项**：

- preferences 持久化在 localStorage，改完需清缓存才生效（`preferences.ts` 文件头注释已写明）。
- 建议先设 10 观察一周，再逐步收到 8。
- 用户会遇到「我开的标签自己没了」，上线前需要通知。
- 这一项本身不解决泄漏，只是限制损失。**不能替代 P0-1 和 P0-2。**

---

### P1-1 · 3D 桌宠改按需开启（仅津海通 jht）

> **前置确认：客户是哪个品牌。** 桌宠只在 jht 品牌显示，其他品牌这一项不适用。

**文件**：`apps/web-antd/src/components/jht-mascot/jht-mascot.vue`

```237:254:apps/web-antd/src/components/jht-mascot/jht-mascot.vue
function renderFrame() {
  animationFrameId = window.requestAnimationFrame(renderFrame);
  const delta = Math.min(clock.getDelta(), 0.05);
  // ...
  renderer.render(scene, camera);
}
```

这是全站唯一持续自递归的 `requestAnimationFrame` 循环：**每帧（~60fps）执行一次 WebGL `renderer.render()`**，另外还有骨骼动画 `mixer.update()` 和头部跟随计算。

挂载位置是根组件：

```64:64:apps/web-antd/src/app.vue
      <JhtMascot v-if="shouldShowJhtMascot" />
```

条件是「jht 品牌 + 已登录 + 非 /auth 路由」，也就是**登录后全站常驻**，和当前在哪个页面无关。此外第 442 行还有 `backdrop-filter: blur(8px)`，以及一个未节流的 `window.pointermove` 监听（第 350 行）用于头部跟随。

在低配集显机器上，一个常驻的 60fps WebGL 渲染循环单独就能吃掉可观的 CPU 和 GPU，并且会和页面的正常渲染争抢主线程。

**要做的事**（按代价从小到大）：

1. 默认关闭，改成用户在偏好设置里主动开启（加一个 preferences 开关）。
2. 页面不可见时暂停：监听 `document.visibilitychange`，隐藏时 `cancelAnimationFrame`。
3. 降帧：不必 60fps，桌宠动画 24~30fps 完全够用，用时间累加判断跳帧。
4. `window.pointermove` 加节流（比如 50ms）。

**性能提升**：如果客户是 jht 品牌，关掉桌宠是**单项收益最大**的操作，且改动极小。建议先让客户在浏览器里手动关掉桌宠（组件有关闭按钮，`closeMascot()` 会 `cancelAnimationFrame`）**对比一下卡顿是否消失** —— 这是零成本的验证手段。

---

### P1-2 · 缓存页的轮询与 Observer 停在 `onDeactivated`

同一个根因模式的其他受害者。以下是**已确认**的：

| 位置 | 常驻资源 | 频率 | 所在路由是否 keepAlive |
| --- | --- | --- | --- |
| `views/sea-export-admin/modules/yundang-tracking-panel.vue:333` | `setInterval` 轮询 | 30 秒（最多 20 次） | 是（`SeaExportCreate` / `SeaExportEdit`） |
| `views/air-export-admin/modules/yundang-air-tracking-panel.vue:246` | `setInterval` 轮询 | 30 秒 | 是（`AirExportCreate` / `AirExportEdit`） |
| `views/sea-import-admin/basic-info-form/form.vue` | `ResizeObserver` | 尺寸变化 | 是（进口编辑页） |
| `views/air-export-admin/basic-info-form/form.vue` | `ResizeObserver` | 尺寸变化 | 是（空运编辑页） |

两个轮询面板的清理都只在 `onBeforeUnmount`，缓存后继续在后台发请求。虽然后端很快、单次开销小，但属于同类问题，一起改成本很低。

`views/sea-export-admin/basic-info-form/form.vue` 和 `views/sea-export-admin/changeOrder/index.vue` **已经有** `onDeactivated`，可以作为参考实现。

#### 需要逐个复核的清单

下列文件含 `addEventListener` / `ResizeObserver` / `MutationObserver` / `setInterval`，且所在路由可能是 keepAlive。**未逐个核实**，建议按第四节的检查清单过一遍：

- `views/_shared/order-fee/modules/order-fee-table-handsontable.vue`
- `views/_shared/order-fee/modules/OrderFeeTableCore.vue`
- `views/_shared/order-fee/modules/order-fee-table.vue`
- `views/sea-export-admin/freight-rate/list.vue`
- `views/sea-export-admin/freight-rate/modules/{form,editForm,sync-update-form}.vue`
- `views/system/basic-data/OrderFeeTemplateAdmin/list.vue`
- `views/bank-statement/form.vue`
- `views/bank-statement/components/receive-settlement-panel.vue`
- `views/audit-approval/expense-all/modules/detail.vue`
- `components/list-grouping/grouping-tabs.vue`（被各列表页的分组侧栏使用）
- `components/nested-data-table/nested-data-table.vue`
- `utils/table-column-resize.ts`
- `packages/effects/common-ui/src/components/ellipsis-text/ellipsis-text.vue`（如果在表格单元格里大量使用，实例数会很多）
- `packages/effects/common-ui/src/components/resize/resize.vue`

---

### P2-1 · 表格虚拟滚动

> 收益大，但优化的是「打开列表的那一下」，不是「一直慢」。放在 P0/P1 之后。

**现状**：全站 61 个 vxe 列表页**完全没有虚拟滚动**（`apps/web-antd/src` 内搜不到任何 `scrollY` / `scroll-y` 配置）。

| 页面                        | 列数 | 默认每页   | 首屏单元格 |
| --------------------------- | ---- | ---------- | ---------- |
| `sea-export-admin/data.ts`  | 57   | 20         | 1,140      |
| `sea-import-admin/data.ts`  | 57   | 20         | 1,140      |
| `schedule-query/data.ts`    | 53   | **100**    | **5,300**  |
| `air-export-admin/data.ts`  | 48   | 20         | 960        |
| `_shared/order-fee/data.ts` | 28   | **无分页** | 全部费用行 |

`packages/effects/plugins/src/vxe-table/use-vxe-grid.vue:1977` 允许的 `pageSizes` 最大到 **200**（默认 `pageSize: 20` 在 1975 行），海运列表调到 200 就是 11,400 个单元格一次性挂载。

**要做的事**：

1. `apps/web-antd/src/adapter/vxe-table.ts:186` 的 `vxeUI.setConfig({ grid: {...} })` 里加 `scrollY: { enabled: true, gt: 20 }` 和 `scrollX: { enabled: true, gt: 20 }`。
2. **先只在一个页面验证**（建议海运出口列表），确认无异常再放开全局。
3. 收敛分页上限：`use-vxe-grid.vue:1977` 的 `pageSizes` 上限收到 100；`schedule-query/list.vue:66` 默认 pageSize 由 100 降到 50，去掉 500 / 1000 选项。
4. 关掉不需要的 `keepSource`。全站 65 处 `keepSource: true`，只有需要 `revert` / `getUpdateRecords` 的可编辑表格用得上。

**已知冲突点（风险来源）**：

- **`height: 'auto'` 与虚拟滚动冲突**。列表页普遍这么写（如 `sea-export-admin/list.vue:309`），必须改成固定高度或 `maxHeight`，否则加了 `scrollY` 也不生效。
- 固定列（`fixed`）、行合并（`span-method`）、自定义 `slots` 需重新验证。
- 全局 `showOverflow: true` 已开启，这是虚拟滚动的前提，**不要关**。
- 全站 117 处 `cellRender` + 约 30 处 `slots`，订单费用表 28 列里约 20 列渲染 `a-select` / `a-input` 级组件。虚拟滚动后这些只在可视区创建，收益最明显，也最需要验证。

**性能提升**：列表首屏渲染的 DOM 单元格 1,140 → 约 180（**−84%**）；200 条/页时 11,400 → 180（**−98%**）；船期查询 5,300 → 180（**−97%**）。对应打开耗时预计 −70% ~ −85%（估算，需实测校准）。

---

### P2-2 · 费用表 deep watch 触发全量重载

**文件**：`apps/web-antd/src/views/_shared/order-fee/modules/order-fee-table-handsontable.vue:716`

```ts
watch(
  () => dataSource.value,
  (newData) => {
    hotSettings.value.data = newData;
    nextTick(() => {
      hotInstance.loadData(newData);
    });
  },
  { deep: true },
);
```

费用行**没有分页**，任意一个嵌套字段变一下就整表 `loadData`。该文件共 6 处 deep watch。

**要做的事**：监听长度或自增版本号代替 `deep: true`；单元格级改动用 `hot.setDataAtCell` 做增量更新。

**顺带清理**：全库 31 处 `watch(..., { deep: true })`、8 处 `JSON.parse(JSON.stringify(...))` 深拷贝（`OrderFeeTemplateAdmin/edit.vue` 3 处、`workflow/modules/node-wrap.vue` 3 处，可换 `structuredClone`）、约 8 处在 `v-for` 里调用函数（`workflow-timeline.vue`、`nested-data-table.vue`、`port-congestion/list.vue`，应改 `computed`）。

---

### P2-3 · 收敛 vxe 全局渲染开销

**文件**：`apps/web-antd/src/adapter/vxe-table.ts:186`

全局同时开了 `stripe: true`（斑马纹）、`showOverflow: true`（溢出 tooltip）、`rowConfig.isHover: true`（行悬停高亮），影响全部 61 个列表页。

- `showOverflow` 是虚拟滚动的前提，**保留**。
- `stripe` 和 `isHover` 属于纯视觉开销。`isHover` 尤其值得试 —— 鼠标在 57 列的大表格上移动时会持续触发行样式切换，而这又会触发 P0-1 里那个 MutationObserver。

**要做的事**：做完 P0 和 P2-1 后如果仍卡，再关这两项试试。属于「最后的调优手段」，不要一上来就改（会影响所有人的视觉习惯）。

---

## 四、根因模式与通用修法

### 为什么 `onUnmounted` 是死代码

```
路由切走  → onDeactivated  ✅ 触发
          → onUnmounted    ❌ 不触发（组件被 KeepAlive 保留）

手动关标签 → onUnmounted    ✅ 触发
maxCount 淘汰 → onUnmounted ✅ 触发（但当前 maxCount = 0，永不淘汰）
```

### 通用修法模板

```ts
import { onActivated, onDeactivated, onMounted, onUnmounted } from 'vue';

let stopped = true;

function start() {
  if (!stopped) return; // 幂等，防重复注册
  stopped = false;
  window.addEventListener('resize', handleResize);
  observer = new ResizeObserver(handleResize);
  observer.observe(containerRef.value!); // 注意：观察容器，不是 document.body
  timer = setInterval(poll, 30_000);
}

function stop() {
  if (stopped) return;
  stopped = true;
  window.removeEventListener('resize', handleResize); // 同一个函数引用
  observer?.disconnect();
  observer = null;
  if (timer) {
    clearInterval(timer);
    timer = null;
  }
}

onMounted(start);
onActivated(start); // 从缓存恢复
onDeactivated(stop); // 切走立刻停
onUnmounted(stop); // 真正销毁兜底
```

要点：

1. `start` / `stop` 必须**幂等**，因为 `onMounted` 和 `onActivated` 在首次挂载时可能都会触发。
2. `removeEventListener` 必须传**与注册时同一个函数引用**。匿名箭头函数是本项目已出现的错误（P0-1 问题 1）。
3. Observer 的目标要尽可能小 —— 观察自己的容器，而不是 `document.body` + `subtree`。

### Code Review 检查清单

新增或修改组件时问这五个问题：

1. 这个组件所在的路由有没有 `meta.keepAlive: true`？
2. 有没有用 `addEventListener` / `new ResizeObserver` / `new MutationObserver` / `setInterval` / `requestAnimationFrame`？
3. 清理是不是只写在 `onUnmounted` / `onBeforeUnmount`？（→ 需要补 `onDeactivated`）
4. `removeEventListener` 传的引用和 `addEventListener` 是不是同一个？
5. 监听 / 观察的范围是不是过大（`window`、`document`、`document.body` + `subtree: true`）？能不能缩到组件容器？

建议把这五条加进 `.cursor/rules` 或团队的 PR 模板。

---

## 五、建议实施顺序

| 阶段 | 做什么 | 为什么这个顺序 |
| --- | --- | --- |
| **第 0 步（零成本）** | 让客户手动关掉桌宠（如果是 jht 品牌）；或让客户**不打开报表页**用半天，对比卡顿 | 不改一行代码就能定位主因，值得先做 |
| **第 1 天** | **P0-1** 报表页三处泄漏 | 最符合「一直慢」现象，影响全站，改动局限在一个文件 |
| **第 1 天** | **P0-3** `maxCount: 8` | 10 分钟，限制损失面 |
| **第 2 天** | **P0-2** vxe-grid click 监听器 | 框架层改动，需回归表格字段搜索弹层 |
| **第 2 天** | **P1-1** 桌宠（若客户是 jht） | 单项收益可能最大，改动小 |
| **第 3 天** | **P1-2** 其余缓存页的轮询与 Observer + 逐个复核清单 | 同一模式批量处理 |
| **第 2 周** | **P2-1** 虚拟滚动逐页验证 | 唯一的大工程，单独排期 |
| **视情况** | **P2-2 / P2-3** | 做完上面再评估还差多少 |
| **另行排期** | 附录 B 的冷加载优化 | 与「一直慢」无关，但发版后首次打开的体验值得改 |

---

## 六、验证方法

### 第一步：先证实是不是这几个原因（不改代码）

在客户机器或同配置机器上，按顺序做：

**A. 验证报表页泄漏（对应 P0-1）**

1. 登录，直接进海运出口列表，操作 5 分钟，记录主观流畅度。
2. **不要刷新**，打开利润报表，查一次数据。
3. 切回海运出口列表，做同样的操作，对比是否明显变慢。
4. 关掉报表标签，再对比一次（如果还是慢，说明是 keepAlive 缓存导致的泄漏，与本文判断一致）。
5. 最后刷新浏览器（F5），再对比 —— 如果刷新后恢复，基本可以确认是运行时泄漏而不是别的原因。

**B. 数一数监听器有没有累积（对应 P0-2）**

在 Chrome DevTools 的 Console 里执行（`getEventListeners` 只在 DevTools Console 可用）：

```js
getEventListeners(window).click?.length;
getEventListeners(window).resize?.length;
```

- 刚登录时记一个基数。
- 依次打开 5 个列表页标签，再看 `click` 的数量有没有跟着涨。
- 打开两次报表页，看 `resize` 有没有涨（涨了就证实 P0-1 问题 1）。

**C. 验证桌宠（对应 P1-1，仅 jht）**

点掉桌宠的关闭按钮，操作 5 分钟对比。桌宠的 `closeMascot()` 会 `cancelAnimationFrame`，所以关掉即刻生效。

### 第二步：Performance 面板坐实

DevTools → Performance，**务必开 CPU 4x slowdown** 模拟低配机，录制 10 秒「在列表页正常操作」：

- 看 **Main** 轨道有没有密集的周期性长任务（每秒一根 → 定时器；每 16ms 一根 → RAF 循环）。
- 在 Bottom-Up 里按 Self Time 排序，找占比最高的函数。如果 `MutationObserver` 回调或 `onDocumentClick` 排前面，直接印证 P0-1 / P0-2。
- 看 **GPU** 轨道是否持续繁忙（→ 桌宠或毛玻璃）。

### 第三步：Memory 面板看缓存驻留

DevTools → Memory → Heap snapshot：

- 依次打开 10 个列表标签，各拍一次快照，看 JS Heap 是否线性增长且不回落。
- 在快照里搜 `Detached` 找游离 DOM，搜 `HTMLTableCellElement` 看单元格实例数量。

---

## 七、待确认事项

| # | 事项 | 影响 | 谁来定 |
| --- | --- | --- | --- |
| 1 | **客户是哪个品牌** | 决定 P1-1（桌宠）适不适用；jht 的话它可能是主因 | 需确认 |
| 2 | 客户日常会不会打开报表页 | 决定 P0-1 是不是主因 | 问客户 |
| 3 | 客户习惯同时开多少个标签 | 决定 P0-2 / P0-3 的权重 | 问客户 |
| 4 | 客户机器的具体配置（CPU 型号、内存、是否集显、浏览器版本） | 决定优化目标定在哪 | 需收集 |
| 5 | 标签页上限设几个 | 建议先 10 再收到 8 | 业务，需提前通知用户 |
| 6 | 桌宠是产品要求还是可选功能 | 决定是默认关闭还是只做降帧 | 业务 |

---

## 附录 A：已排除的方向

这几个方向查过了，**不是**「一直慢」的原因。记录在此避免重复排查。

| 方向 | 结论 | 依据 |
| --- | --- | --- |
| Pinia store 每次变更都 AES 加密写 localStorage | **不是** | `packages/stores/src/setup.ts:20` 确实用 SecureLS + AES，但只有 3 个 store 开了 persist：`core-access`（token / 锁屏，登录登出时才变）、`core-timezone`（改时区时）、`core-tabbar`（写 **sessionStorage 明文**）。业务 store（`auth.ts`、`table-config.ts`、`data-refresh.ts`）均无 persist。不构成每次交互的开销。 |
| 工作台 1 秒定时器 | **不是** | `workbench-emergency-queue.vue:55` 确实是 1 秒周期，但 `Workspace` 路由**没有** `keepAlive`（只有 `affixTab: true`），离开即销毁。 |
| 3D 地球看板（hhyy 默认首页） | **仅页面级** | `views/dashboard/sea-freight-globe/index.vue` 有 WebGL 自转（`autoRotateSpeed = 0.45`）和 8 处 `backdrop-filter: blur()`，但 `SeaFreightGlobe` 路由**没有** `keepAlive`，`onBeforeUnmount` 会 `renderer.dispose()`。只在停留该页时耗 GPU，不影响全站。 |
| 全站无限 CSS 动画 / 大范围毛玻璃 | **不存在** | 仅 nprogress 进度条和 loading spinner 有 `animation: infinite`，都是短暂场景。 |
| 未节流的全局 scroll / mousemove | **基本不存在** | 唯一例外是桌宠的 `window.pointermove`（jht 专属，见 P1-1）。 |
| `setTimeout` 无限递归 | **不存在** | 全库未发现。 |
| Tabbar `updateCacheTabs` | **可忽略** | `packages/stores/src/modules/tabbar.ts:502`，每次路由切换 O(标签数)，通常 <20。 |
| Service Worker | **不存在影响** | `utils/register-oss-cache-sw.ts:6` 当前逻辑是在 `window.load` 后**注销** SW 并清缓存，不拦截请求。 |
| 生产环境带 Vue DevTools | **不存在** | `.env.hhyy` 里 `VITE_DEVTOOLS=true`，但插件条件是 `!isBuild && devtools`（`internal/vite-config/src/plugins/index.ts:68`），生产构建不会注入。 |
| 后端接口慢 | **不是** | 用户明确反馈接口很快。 |

---

## 附录 B：冷加载优化（v1 内容，降级保留）

以下方案**与「一直慢」无关**，只影响首次访问、发版后、清缓存后的首屏加载。数据是实测的，仍然值得做，但优先级在 P0/P1/P2 之后。

### 实测基线

| 基线 | 实测值 | 构成 |
| --- | --- | --- |
| 冷加载总下载量 | 28.4 MB | 中文字体 24 MB + 首屏 JS 4.72 MB + 全局 CSS 0.33 MB |
| 每次加载的 JS 解析量 | 4.72 MB | 入口 921 KB + bootstrap 2,046 KB + 布局 1,214 KB + Handsontable 540 KB |

### B-1 字体瘦身（冷加载 −72% ~ −83%）

`apps/web-antd/src/utils/global-font-loader.ts:9` 注入 6 个字重的阿里巴巴普惠体，每个约 8 MB **未压缩 TTF**，合计 **47.99 MB**（实测）。响应头 `Content-Encoding` 为空、**没有 `Cache-Control`**、`cf-cache-status: DYNAMIC`（CDN 不缓存）。浏览器实际取约 3 个字重 ≈ 24 MB，并为每个字面常驻约 8 MB 内存。

三档做法：

- **B-1a（10 分钟）**：6 个字重砍到 2 个（400 / 600），48 MB → 16 MB，−67%，格式不动。**建议先做这一步止血**，顺带也减 16 MB 内存占用 —— 这一点对低配机的「一直慢」也有间接帮助。
- **B-1b（1 小时）**：删掉 `initGlobalFonts()` 调用，回落到 `Microsoft YaHei`（Windows 自带），冷加载 −83%。需业务确认观感。
- **B-1c（半天）**：用 `pyftsubset` 把 400 / 600 子集化为 woff2（GB2312 常用字），每个约 1.2–1.8 MB，并加 `Cache-Control: public, max-age=31536000, immutable`。

**注意**：改字体要同步三处 —— `global-font-loader.ts:9`、`global-font.css`、`app.vue:33`（`globalFontFamily` 常量，喂给 antd 的 `token.fontFamily`，最容易漏）。

### B-2 开启传输压缩（JS + CSS 传输 −72%）

8 个品牌 env 文件全是 `VITE_COMPRESS=none`（`.env.hhyy:14`、`.env.jht:10`、`.env.production:9`、`.env.jiayue:13`、`.env.jytest:14`、`.env.longshan:13`、`.env.sjtd:10`、`.env.demo:13`），仓库内也没有 `web.config`。

**先验证再动手**（1 分钟）：

```bash
curl -s -D - -o /dev/null -H "Accept-Encoding: gzip, br, deflate" \
  http://<前端站点>/jse/index-index-<hash>.js
```

有 `content-encoding: gzip` → IIS 已开，跳过此项。没有 → 值得做。

优先让 IIS 自己压（前端零改动，需服务器侧 `httpCompression` 配置 + 站点 `web.config` 的 `urlCompression`）；备选是 `VITE_COMPRESS=brotli,gzip` 加 URL Rewrite 规则，代价是 dist 文件数从 875 涨到约 2,500。

顺带建议给带 hash 的产物加长缓存（`js/`、`css/`、`jse/` 目录设 `Cache-Control: max-age=31536000`），但 `index.html` 必须 `no-cache`，否则发版后客户端拿不到新版本。

### B-3 拆掉启动阻塞（−1.0 ~ −2.5 秒）

- `main.ts:18` 的 `MIN_APP_LOADING_MS = 800`：不管多快都要凑满 800ms。改为 0（或 150 防闪）。保留后面的双帧 `requestAnimationFrame`，那不是人为延迟。
- `bootstrap.ts:61` 的 `await initEnumCache(true)`：`true` 会跳过 localStorage，强制并行发 6 个枚举请求并阻塞 `mount`。

  改法必须三步一起，否则枚举永不更新：① 挂载前改 `initEnumCache(false)` 用缓存；② `app.mount()` 之后 `void initEnumCache(true)` 后台静默刷新；③ 恢复 `utils/init-enum.ts:208-213` 被注释掉的 TTL 检查。

### B-4 重库移出首屏（JS 解析量 −30%）

- `main.ts:9-14` 顶层 `registerAllModules()` 把 Handsontable 全量模块 + 2 个 CSS 拉进启动链，而全站只有 5 处用得到（`order-fee/modules/OrderFeeTableCore.vue:6`、`report/_shared/report-hot-table.vue:4`、`client/contact/contact-handsontable.vue:30`、`OrderFeeTemplateAdmin/modules/order-fee-template-table.vue:3`、`freight-rate/modules/BatchAddTableCore.vue:3`）。改成懒注册。
- `app.vue:11` 静态引入 `JhtMascot`，把 three.js 拖进了 2,046 KB 的 `bootstrap` chunk（产物里能搜到 `WebGLRenderer`）。改 `defineAsyncComponent`。**这一项和 P1-1 是同一个组件，可以一起做。**
- `views/client/payment-terms/data.ts:17` 有一行 `import { color } from 'three/tsl'` 属于误导入，文件里根本没用到，直接删。

### 首屏关键链路实测明细

| chunk                  | 原始体积     | gzip 后      | 说明         |
| ---------------------- | ------------ | ------------ | ------------ |
| `jse/index-index-*.js` | 921 KB       | 260 KB       | 入口         |
| `js/bootstrap-*.js`    | 2,046 KB     | 601 KB       | 含 three.js  |
| `js/index-Cf1ji_kp.js` | 1,214 KB     | 362 KB       | 布局         |
| `js/base-*.js`         | 540 KB       | 114 KB       | Handsontable |
| `css/index-*.css`      | 329 KB       | 62 KB        | 全局 CSS     |
| **合计**               | **5,050 KB** | **1,399 KB** |              |

### 体积验证脚本

```powershell
pnpm -F @vben/web-antd run build:hhyy

$d = 'apps/web-antd/dist'
$all = Get-ChildItem $d -Recurse -File
"TOTAL: {0:N2} MB / {1} files" -f (($all | Measure-Object Length -Sum).Sum/1MB), $all.Count
$all | Where-Object Extension -eq '.js' | Sort-Object Length -Descending |
  Select-Object -First 10 |
  ForEach-Object { "{0,9:N0} KB  {1}" -f ($_.Length/1KB), $_.Name }
```

注意：`build:hhyy` 会把 hhyy 品牌图片同步进 `apps/web-antd/public/`（`vite.config.mts` 的 `sync-loading-logo` 插件行为），跑完记得 `git checkout -- apps/web-antd/public` 还原。

---

**数据来源**：代码位置全部核对到行号；体积来自本机 `pnpm -F @vben/web-antd run build:hhyy` 真实产物（exit code 0）+ 本地 gzip 实测；字体体积来自对 `oss.jiayuebetter.com` 的 HEAD 请求实测。运行时部分为静态代码分析推断，**各原因的实际占比需按第六节做一次 Performance 录制确认**。
